//! Generated by tsz compiler — d83_array_methods [logic.zig]
//! Source: d83_array_methods.tsz


// [origin:d83_array_methods:logic]
// ── Embedded JS logic ────────────────────────────────────────
pub const JS_LOGIC =
    \\var items = [];
    \\// OA initial data
    \\setItems([ { name : "" , value : 0 } ]);
    \\var output = '';
    \\function setOutput(v) { output = v; __setStateString(0, v); }
    \\var count = 0;
    \\function setCount(v) { count = v; __setState(1, v); }
    \\var original = [
    \\  { name: 'Zulu', value: 5 },
    \\  { name: 'Alpha', value: 30 },
    \\  { name: 'Mike', value: 15 },
    \\  { name: 'Bravo', value: 25 },
    \\  { name: 'Yankee', value: 10 },
    \\  { name: 'Delta', value: 20 }
    \\];
    \\setItems(original);
    \\setCount(original.length);
    \\
    \\function sortByName() {
    \\  var list = original.slice();
    \\  for (var i = 0; i < list.length; i++) {
    \\    for (var j = i + 1; j < list.length; j++) {
    \\      if (list[j].name < list[i].name) {
    \\        var tmp = list[i]; list[i] = list[j]; list[j] = tmp;
    \\      }
    \\    }
    \\  }
    \\  setItems(list);
    \\  setOutput('sorted by name');
    \\}
    \\
    \\function sortByValue() {
    \\  var list = original.slice();
    \\  for (var i = 0; i < list.length; i++) {
    \\    for (var j = i + 1; j < list.length; j++) {
    \\      if (list[j].value > list[i].value) {
    \\        var tmp = list[i]; list[i] = list[j]; list[j] = tmp;
    \\      }
    \\    }
    \\  }
    \\  setItems(list);
    \\  setOutput('sorted by value desc');
    \\}
    \\
    \\function filterAbove(threshold) {
    \\  var result = [];
    \\  for (var i = 0; i < original.length; i++) {
    \\    if (original[i].value > threshold) result.push(original[i]);
    \\  }
    \\  setItems(result);
    \\  setCount(result.length);
    \\  setOutput('filtered > ' + threshold + ': ' + result.length + ' items');
    \\}
    \\
    \\function findByName(name) {
    \\  for (var i = 0; i < original.length; i++) {
    \\    if (original[i].name === name) {
    \\      setOutput('found ' + name + ' at index ' + i + ' (value=' + original[i].value + ')');
    \\      return;
    \\    }
    \\  }
    \\  setOutput(name + ' not found');
    \\}
    \\
    \\function reverseList() {
    \\  var list = original.slice();
    \\  var reversed = [];
    \\  for (var i = list.length - 1; i >= 0; i--) {
    \\    reversed.push(list[i]);
    \\  }
    \\  setItems(reversed);
    \\  setOutput('reversed');
    \\}
    \\
    \\function showAll() {
    \\  setItems(original);
    \\  setCount(original.length);
    \\  setOutput('showing all');
    \\}
    \\function setItems(v) { items = v; __setObjArr0(v); }
    \\if (items && items.length > 0) setItems(items);
    \\
;

// [origin:d83_array_methods:logic]
// ── Embedded Lua logic ───────────────────────────────────────
pub const LUA_LOGIC =
    \\
;

