//! Generated by tsz compiler — d10_handler_triple_capture [logic.zig]
//! Source: d10_handler_triple_capture.tsz


// [origin:d10_handler_triple_capture:logic]
// ── Embedded JS logic ────────────────────────────────────────
pub const JS_LOGIC =
    \\var entries = [];
    \\// OA initial data
    \\setEntries([ { label : "" , weight : 0 , category : 0 } ]);
    \\var multiplier = 1;
    \\function setMultiplier(v) { multiplier = v; __setState(0, v); }
    \\var accumulator = 0;
    \\function setAccumulator(v) { accumulator = v; __setState(1, v); }
    \\var lastIdx = 0;
    \\function setLastIdx(v) { lastIdx = v; __setState(2, v); }
    \\var lastWeight = 0;
    \\function setLastWeight(v) { lastWeight = v; __setState(3, v); }
    \\var clickLog = 0;
    \\function setClickLog(v) { clickLog = v; __setState(4, v); }
    \\// Populate entries for triple capture test
    \\
    \\var entries = [];
    \\var labels = ['Mercury', 'Venus', 'Earth', 'Mars', 'Jupiter',
    \\              'Saturn', 'Uranus', 'Neptune', 'Pluto', 'Ceres',
    \\              'Eris', 'Haumea', 'Makemake', 'Sedna', 'Quaoar'];
    \\
    \\for (var i = 0; i < 15; i++) {
    \\  entries.push({
    \\    label: labels[i],
    \\    weight: (i + 1) * 3,
    \\    category: i % 3
    \\  });
    \\}
    \\setEntries(entries);
    \\
    \\
    \\
    \\function setEntries(v) { entries = v; __setObjArr0(v); }
    \\if (entries && entries.length > 0) setEntries(entries);
    \\function __mapPress_0_0(idx) {
    \\  var entry = entries[idx];
    \\  var idx = idx;
    \\  var weight = entry.weight;
    \\  setAccumulator(accumulator + entry.weight * multiplier + idx); setLastIdx(idx); setLastWeight(entry.weight); setClickLog(clickLog + 1);
    \\}
    \\
;

// [origin:d10_handler_triple_capture:logic]
// ── Embedded Lua logic ───────────────────────────────────────
pub const LUA_LOGIC =
    \\
;

