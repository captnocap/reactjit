//! Generated by tsz compiler — d27a_array_methods_script [logic.zig]
//! Source: d27a_array_methods_script.tsz


// [origin:d27a_array_methods_script:logic]
// ── Embedded JS logic ────────────────────────────────────────
pub const JS_LOGIC =
    \\var items = [];
    \\var filtered = [];
    \\// OA initial data
    \\setItems([ { name : "" , category : "" , price : 0 , qty : 0 } ]);
    \\setFiltered([ { name : "" , category : "" , price : 0 , qty : 0 } ]);
    \\var sortDir = 0;
    \\function setSortDir(v) { sortDir = v; __setState(0, v); }
    \\var totalValue = 0;
    \\function setTotalValue(v) { totalValue = v; __setState(1, v); }
    \\var matchCount = 0;
    \\function setMatchCount(v) { matchCount = v; __setState(2, v); }
    \\var activeFilter = '';
    \\function setActiveFilter(v) { activeFilter = v; __setStateString(3, v); }
    \\var data = [
    \\  { name: 'Hammer', category: 'tool', price: 25, qty: 4 },
    \\  { name: 'Apples', category: 'food', price: 3, qty: 12 },
    \\  { name: 'Wrench', category: 'tool', price: 18, qty: 7 },
    \\  { name: 'Bread', category: 'food', price: 4, qty: 5 },
    \\  { name: 'Drill', category: 'tool', price: 89, qty: 2 },
    \\  { name: 'Cheese', category: 'food', price: 7, qty: 8 },
    \\  { name: 'Pliers', category: 'tool', price: 12, qty: 6 },
    \\  { name: 'Rice', category: 'food', price: 2, qty: 20 },
    \\  { name: 'Saw', category: 'tool', price: 35, qty: 3 },
    \\  { name: 'Milk', category: 'food', price: 5, qty: 10 }
    \\];
    \\setItems(data);
    \\setFiltered(data);
    \\recalc(data);
    \\
    \\function recalc(list) {
    \\  var total = 0;
    \\  for (var i = 0; i < list.length; i++) {
    \\    total += list[i].price * list[i].qty;
    \\  }
    \\  setTotalValue(total);
    \\  setMatchCount(list.length);
    \\}
    \\
    \\function filterBy(cat) {
    \\  setActiveFilter(cat);
    \\  var result;
    \\  if (cat === '') {
    \\    result = items.slice();
    \\  } else {
    \\    result = [];
    \\    for (var i = 0; i < items.length; i++) {
    \\      if (items[i].category === cat) result.push(items[i]);
    \\    }
    \\  }
    \\  setFiltered(result);
    \\  recalc(result);
    \\}
    \\
    \\function sortByPrice() {
    \\  var list = filtered.slice();
    \\  for (var i = 0; i < list.length; i++) {
    \\    for (var j = i + 1; j < list.length; j++) {
    \\      if (sortDir === 0 && list[j].price < list[i].price) {
    \\        var tmp = list[i]; list[i] = list[j]; list[j] = tmp;
    \\      }
    \\      if (sortDir === 1 && list[j].price > list[i].price) {
    \\        var tmp2 = list[i]; list[i] = list[j]; list[j] = tmp2;
    \\      }
    \\    }
    \\  }
    \\  setSortDir(sortDir === 0 ? 1 : 0);
    \\  setFiltered(list);
    \\}
    \\
    \\function sortByName() {
    \\  var list = filtered.slice();
    \\  for (var i = 0; i < list.length; i++) {
    \\    for (var j = i + 1; j < list.length; j++) {
    \\      if (sortDir === 0 && list[j].name < list[i].name) {
    \\        var tmp = list[i]; list[i] = list[j]; list[j] = tmp;
    \\      }
    \\      if (sortDir === 1 && list[j].name > list[i].name) {
    \\        var tmp2 = list[i]; list[i] = list[j]; list[j] = tmp2;
    \\      }
    \\    }
    \\  }
    \\  setSortDir(sortDir === 0 ? 1 : 0);
    \\  setFiltered(list);
    \\}
    \\function setItems(v) { items = v; __setObjArr0(v); }
    \\function setFiltered(v) { filtered = v; __setObjArr1(v); }
    \\if (items && items.length > 0) setItems(items);
    \\if (filtered && filtered.length > 0) setFiltered(filtered);
    \\
;

// [origin:d27a_array_methods_script:logic]
// ── Embedded Lua logic ───────────────────────────────────────
pub const LUA_LOGIC =
    \\
;

