//! Generated by tsz compiler — d79_shorthand_properties [logic.zig]
//! Source: d79_shorthand_properties.tsz


// [origin:d79_shorthand_properties:logic]
// ── Embedded JS logic ────────────────────────────────────────
pub const JS_LOGIC =
    \\var items = [];
    \\// OA initial data
    \\setItems([ { name : "" , score : 0 , active : 0 } ]);
    \\var output = '';
    \\function setOutput(v) { output = v; __setStateString(0, v); }
    \\var count = 0;
    \\function setCount(v) { count = v; __setState(1, v); }
    \\var log = [];
    \\
    \\function createItem(name, score, active) {
    \\  // Shorthand: { name, score, active } instead of { name: name, score: score, active: active }
    \\  var item = { name: name, score: score, active: active };
    \\  log.push(item);
    \\  setItems(log);
    \\  setCount(log.length);
    \\  setOutput('added ' + name + ' (' + score + ')');
    \\}
    \\
    \\function buildFromParts() {
    \\  var name = 'Generated';
    \\  var score = 77;
    \\  var active = 1;
    \\  // Mix of shorthand and explicit
    \\  var item = { name: name, score: score, active: active };
    \\  log.push(item);
    \\  setItems(log);
    \\  setCount(log.length);
    \\  setOutput('built from parts');
    \\}
    \\
    \\function clearAll() {
    \\  log = [];
    \\  setItems(log);
    \\  setCount(0);
    \\  setOutput('cleared');
    \\}
    \\
    \\createItem('Alice', 95, 1);
    \\createItem('Bob', 72, 1);
    \\createItem('Carol', 88, 0);
    \\function setItems(v) { items = v; __setObjArr0(v); }
    \\if (items && items.length > 0) setItems(items);
    \\
;

// [origin:d79_shorthand_properties:logic]
// ── Embedded Lua logic ───────────────────────────────────────
pub const LUA_LOGIC =
    \\
;

