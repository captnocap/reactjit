//! Generated by tsz compiler — d85_math_heavy [logic.zig]
//! Source: d85_math_heavy.tsz


// [origin:d85_math_heavy:logic]
// ── Embedded JS logic ────────────────────────────────────────
pub const JS_LOGIC =
    \\var results = [];
    \\// OA initial data
    \\setResults([ { op : "" , value : 0 } ]);
    \\var a = 100;
    \\function setA(v) { a = v; __setState(0, v); }
    \\var b = 7;
    \\function setB(v) { b = v; __setState(1, v); }
    \\var output = '';
    \\function setOutput(v) { output = v; __setStateString(2, v); }
    \\function compute() {
    \\  var log = [];
    \\  log.push({ op: 'a + b', value: a + b });
    \\  log.push({ op: 'a - b', value: a - b });
    \\  log.push({ op: 'a * b', value: a * b });
    \\  log.push({ op: 'a / b (floor)', value: Math.floor(a / b) });
    \\  log.push({ op: 'a % b', value: a % b });
    \\  log.push({ op: 'abs(b - a)', value: Math.abs(b - a) });
    \\  log.push({ op: 'min(a, b)', value: Math.min(a, b) });
    \\  log.push({ op: 'max(a, b)', value: Math.max(a, b) });
    \\  log.push({ op: 'round(a/b)', value: Math.round(a / b) });
    \\  log.push({ op: 'a * a + b * b', value: a * a + b * b });
    \\  setResults(log);
    \\  setOutput('computed ' + log.length + ' ops with a=' + a + ' b=' + b);
    \\}
    \\
    \\compute();
    \\function setResults(v) { results = v; __setObjArr0(v); }
    \\if (results && results.length > 0) setResults(results);
    \\
;

// [origin:d85_math_heavy:logic]
// ── Embedded Lua logic ───────────────────────────────────────
pub const LUA_LOGIC =
    \\
;

