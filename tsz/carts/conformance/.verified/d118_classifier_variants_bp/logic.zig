//! Generated by tsz compiler — d118_classifier_variants_bp [logic.zig]
//! Source: d118_classifier_variants_bp.tsz

const api = @import("framework/api.zig");
const state = api.state;

// [origin:d118_classifier_variants_bp:logic]
// ── Embedded JS logic ────────────────────────────────────────
pub const JS_LOGIC =
    \\var items = [];
    \\// OA initial data
    \\setItems([ { title : "" , detail : "" } ]);
    \\var activeTab = 0;
    \\function setActiveTab(v) { activeTab = v; __setState(0, v); }
    \\const d118_data = 'init';
    \\
    \\function init(state) {
    \\  state.items = [
    \\    { title: 'Dashboard', detail: 'Main overview panel' },
    \\    { title: 'Analytics', detail: 'Usage metrics and graphs' },
    \\    { title: 'Users', detail: 'Account management' },
    \\    { title: 'Logs', detail: 'System event history' },
    \\  ];
    \\}
    \\
    \\
    \\
    \\function setItems(v) { items = v; __setObjArr0(v); }
    \\if (typeof init === 'function') init({ set items(v) { setItems(v); }, set activeTab(v) { setActiveTab(v); } });
    \\if (items && items.length > 0) setItems(items);
    \\function setVariant(v) { __setVariant(v); }
    \\
;

// [origin:d118_classifier_variants_bp:logic]
// ── Embedded Lua logic ───────────────────────────────────────
pub const LUA_LOGIC =
    \\
;

