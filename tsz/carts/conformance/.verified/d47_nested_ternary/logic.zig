//! Generated by tsz compiler — d47_nested_ternary [logic.zig]
//! Source: d47_nested_ternary.tsz


// [origin:d47_nested_ternary:logic]
// ── Embedded JS logic ────────────────────────────────────────
pub const JS_LOGIC =
    \\var level = 0;
    \\function setLevel(v) { level = v; __setState(0, v); }
    \\var mode = 0;
    \\function setMode(v) { mode = v; __setState(1, v); }
    \\var output = '';
    \\function setOutput(v) { output = v; __setStateString(2, v); }
    \\function classify(lvl) {
    \\  var label = lvl === 0 ? 'none' : lvl === 1 ? 'low' : lvl === 2 ? 'mid' : 'high';
    \\  setOutput('level ' + lvl + ' = ' + label);
    \\}
    \\
;

// [origin:d47_nested_ternary:logic]
// ── Embedded Lua logic ───────────────────────────────────────
pub const LUA_LOGIC =
    \\
;

