//! Generated by tsz compiler — d30a_math_derived_script [logic.zig]
//! Source: d30a_math_derived_script.tsz


// [origin:d30a_math_derived_script:logic]
// ── Embedded JS logic ────────────────────────────────────────
pub const JS_LOGIC =
    \\var values = [];
    \\// OA initial data
    \\setValues([ { label : "" , raw : 0 } ]);
    \\var mean = 0;
    \\function setMean(v) { mean = v; __setState(0, v); }
    \\var median = 0;
    \\function setMedian(v) { median = v; __setState(1, v); }
    \\var min = 0;
    \\function setMin(v) { min = v; __setState(2, v); }
    \\var max = 0;
    \\function setMax(v) { max = v; __setState(3, v); }
    \\var range = 0;
    \\function setRange(v) { range = v; __setState(4, v); }
    \\var sum = 0;
    \\function setSum(v) { sum = v; __setState(5, v); }
    \\var count = 0;
    \\function setCount(v) { count = v; __setState(6, v); }
    \\var lastAction = '';
    \\function setLastAction(v) { lastAction = v; __setStateString(7, v); }
    \\var dataset = [
    \\  { label: 'A', raw: 42 },
    \\  { label: 'B', raw: 17 },
    \\  { label: 'C', raw: 88 },
    \\  { label: 'D', raw: 3 },
    \\  { label: 'E', raw: 55 },
    \\  { label: 'F', raw: 71 },
    \\  { label: 'G', raw: 29 },
    \\  { label: 'H', raw: 64 }
    \\];
    \\setValues(dataset);
    \\recalcStats(dataset);
    \\
    \\function recalcStats(data) {
    \\  if (data.length === 0) {
    \\    setMean(0); setMedian(0); setMin(0); setMax(0); setRange(0); setSum(0); setCount(0);
    \\    return;
    \\  }
    \\  var s = 0;
    \\  var lo = data[0].raw;
    \\  var hi = data[0].raw;
    \\  for (var i = 0; i < data.length; i++) {
    \\    s += data[i].raw;
    \\    if (data[i].raw < lo) lo = data[i].raw;
    \\    if (data[i].raw > hi) hi = data[i].raw;
    \\  }
    \\  setSum(s);
    \\  setCount(data.length);
    \\  setMean(Math.round(s / data.length));
    \\  setMin(lo);
    \\  setMax(hi);
    \\  setRange(hi - lo);
    \\
    \\  var sorted = data.slice();
    \\  for (var i = 0; i < sorted.length; i++) {
    \\    for (var j = i + 1; j < sorted.length; j++) {
    \\      if (sorted[j].raw < sorted[i].raw) {
    \\        var tmp = sorted[i]; sorted[i] = sorted[j]; sorted[j] = tmp;
    \\      }
    \\    }
    \\  }
    \\  var mid = Math.floor(sorted.length / 2);
    \\  if (sorted.length % 2 === 0) {
    \\    setMedian(Math.round((sorted[mid - 1].raw + sorted[mid].raw) / 2));
    \\  } else {
    \\    setMedian(sorted[mid].raw);
    \\  }
    \\}
    \\
    \\function addRandom() {
    \\  var label = String.fromCharCode(65 + values.length % 26);
    \\  var val = Math.floor(Math.random() * 100);
    \\  values.push({ label: label, raw: val });
    \\  setValues(values);
    \\  recalcStats(values);
    \\  setLastAction('added ' + label + '=' + val);
    \\}
    \\
    \\function removeFirst() {
    \\  if (values.length === 0) return;
    \\  var removed = values[0];
    \\  var result = [];
    \\  for (var i = 1; i < values.length; i++) {
    \\    result.push(values[i]);
    \\  }
    \\  setValues(result);
    \\  recalcStats(result);
    \\  setLastAction('removed ' + removed.label);
    \\}
    \\
    \\function doubleAll() {
    \\  for (var i = 0; i < values.length; i++) {
    \\    values[i].raw = values[i].raw * 2;
    \\  }
    \\  setValues(values);
    \\  recalcStats(values);
    \\  setLastAction('doubled all');
    \\}
    \\
    \\function normalize() {
    \\  var hi = max;
    \\  if (hi === 0) return;
    \\  for (var i = 0; i < values.length; i++) {
    \\    values[i].raw = Math.round(values[i].raw * 100 / hi);
    \\  }
    \\  setValues(values);
    \\  recalcStats(values);
    \\  setLastAction('normalized to 0-100');
    \\}
    \\function setValues(v) { values = v; __setObjArr0(v); }
    \\if (values && values.length > 0) setValues(values);
    \\
;

// [origin:d30a_math_derived_script:logic]
// ── Embedded Lua logic ───────────────────────────────────────
pub const LUA_LOGIC =
    \\
;

