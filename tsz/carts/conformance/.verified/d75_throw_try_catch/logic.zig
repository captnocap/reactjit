//! Generated by tsz compiler — d75_throw_try_catch [logic.zig]
//! Source: d75_throw_try_catch.tsz


// [origin:d75_throw_try_catch:logic]
// ── Embedded JS logic ────────────────────────────────────────
pub const JS_LOGIC =
    \\var results = [];
    \\// OA initial data
    \\setResults([ { input : "" , status : "" , message : "" } ]);
    \\var errorCount = 0;
    \\function setErrorCount(v) { errorCount = v; __setState(0, v); }
    \\var output = '';
    \\function setOutput(v) { output = v; __setStateString(1, v); }
    \\var log = [];
    \\
    \\function safeParse(input) {
    \\  var status = 'ok';
    \\  var message = '';
    \\  try {
    \\    if (input === '') {
    \\      throw 'empty input';
    \\    }
    \\    if (input === 'bad') {
    \\      throw 'invalid format';
    \\    }
    \\    // Simulate successful parse
    \\    message = 'parsed: ' + input;
    \\  } catch (e) {
    \\    status = 'error';
    \\    message = 'caught: ' + e;
    \\    setErrorCount(errorCount + 1);
    \\  }
    \\  log.push({ input: input, status: status, message: message });
    \\  setResults(log);
    \\  setOutput(message);
    \\}
    \\
    \\function safeDivide(a, b) {
    \\  try {
    \\    if (b === 0) {
    \\      throw 'division by zero';
    \\    }
    \\    var result = Math.floor(a / b);
    \\    setOutput(a + ' / ' + b + ' = ' + result);
    \\  } catch (e) {
    \\    setOutput('error: ' + e);
    \\    setErrorCount(errorCount + 1);
    \\  }
    \\}
    \\
    \\function runAll() {
    \\  log = [];
    \\  setErrorCount(0);
    \\  safeParse('hello');
    \\  safeParse('world');
    \\  safeParse('');
    \\  safeParse('bad');
    \\  safeParse('ok');
    \\}
    \\
    \\runAll();
    \\function setResults(v) { results = v; __setObjArr0(v); }
    \\if (results && results.length > 0) setResults(results);
    \\
;

// [origin:d75_throw_try_catch:logic]
// ── Embedded Lua logic ───────────────────────────────────────
pub const LUA_LOGIC =
    \\
;

