//! Generated by tsz compiler — d26a_timer_script [logic.zig]
//! Source: d26a_timer_script.tsz


// [origin:d26a_timer_script:logic]
// ── Embedded JS logic ────────────────────────────────────────
pub const JS_LOGIC =
    \\var ticks = 0;
    \\function setTicks(v) { ticks = v; __setState(0, v); }
    \\var running = 0;
    \\function setRunning(v) { running = v; __setState(1, v); }
    \\var message = '';
    \\function setMessage(v) { message = v; __setStateString(2, v); }
    \\var elapsed = 0;
    \\function setElapsed(v) { elapsed = v; __setState(3, v); }
    \\var timerId = null;
    \\var startTime = 0;
    \\
    \\function startTimer() {
    \\  if (running === 1) return;
    \\  setRunning(1);
    \\  startTime = Date.now();
    \\  setMessage('Timer started');
    \\  timerId = setInterval(function() {
    \\    setTicks(ticks + 1);
    \\    var secs = Math.floor((Date.now() - startTime) / 1000);
    \\    setElapsed(secs);
    \\  }, 500);
    \\  setTimeout(function() {
    \\    setMessage('5s milestone reached');
    \\  }, 5000);
    \\}
    \\
    \\function stopTimer() {
    \\  if (timerId !== null) {
    \\    clearInterval(timerId);
    \\    timerId = null;
    \\  }
    \\  setRunning(0);
    \\  setMessage('Timer stopped at ' + ticks + ' ticks');
    \\}
    \\
    \\function resetTimer() {
    \\  stopTimer();
    \\  setTicks(0);
    \\  setElapsed(0);
    \\  setMessage('Timer reset');
    \\}
    \\
;

// [origin:d26a_timer_script:logic]
// ── Embedded Lua logic ───────────────────────────────────────
pub const LUA_LOGIC =
    \\
;

