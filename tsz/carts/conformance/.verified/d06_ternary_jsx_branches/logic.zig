//! Generated by tsz compiler — d06_ternary_jsx_branches [logic.zig]
//! Source: d06_ternary_jsx_branches.tsz


// ── Embedded JS logic ──
pub const JS_LOGIC =
    \\var mode = 0;
    \\function setMode(v) { mode = v; }
    \\var taps = 0;
    \\function setTaps(v) { taps = v; }
    \\
;

// ── Embedded Lua logic ──
pub const LUA_LOGIC =
    \\local _state = {}
    \\function _getState(k) return _state[k] end
    \\function _setState(k, v) _state[k] = v; __markDirty() end
    \\
    \\function __mapLoop(arr, fn)
    \\  if not arr then return nil end
    \\  local r = {}
    \\  for i, item in ipairs(arr) do
    \\    r[#r + 1] = fn(item, i)
    \\  end
    \\  r.__isMapResult = true
    \\  return r
    \\end
    \\
    \\function __flattenChildren(children)
    \\  if not children then return {} end
    \\  local flat = {}
    \\  for _, child in ipairs(children) do
    \\    if type(child) == "table" and child.__isMapResult then
    \\      for _, mc in ipairs(child) do flat[#flat + 1] = mc end
    \\    elseif child ~= nil then
    \\      flat[#flat + 1] = child
    \\    end
    \\  end
    \\  return flat
    \\end
    \\
    \\_state["mode"] = 0
    \\mode = _state["mode"]
    \\function setMode(v) _state["mode"] = v; mode = v; __markDirty() end
    \\_state["taps"] = 0
    \\taps = _state["taps"]
    \\function setTaps(v) _state["taps"] = v; taps = v; __markDirty() end
    \\
    \\function App()
    \\  return { style = { background_color = 0x1a1a1a, padding = 20, width = -1, height = -1, gap = 16, overflow = "hidden" }, children = {
    \\    { style = { background_color = 0xff4757, border_radius = 8, padding = 12, align_self = "start" }, lua_on_press = "setMode(mode == 0 and 1 or 0)", children = {
    \\      { text = (mode == 0) and "Switch to B" or "Switch to A", font_size = 14, text_color = 0xffffff }
    \\    } },
    \\    (mode == 0) and { style = { background_color = 0x2f3542, border_radius = 12, padding = 20, gap = 12 }, children = {
    \\      { text = "Branch A", font_size = 20, text_color = 0xff6b81 },
    \\      { style = { flex_direction = "row", gap = 8 }, children = {
    \\        { style = { width = 40, height = 40, background_color = 0xff4757, border_radius = 20 } },
    \\        { style = { width = 40, height = 40, background_color = 0xff6348, border_radius = 8 } },
    \\        { style = { width = 40, height = 40, background_color = 0xffa502, border_radius = 4 } }
    \\      } },
    \\      { style = { padding = 8 }, lua_on_press = "setTaps(taps + 1)", children = {
    \\        { text = "A taps: " .. tostring(taps), font_size = 12, text_color = 0x747d8c }
    \\      } }
    \\    } } or nil,
    \\    (not (mode == 0)) and { style = { background_color = 0x2f3542, border_radius = 12, padding = 20, gap = 8 }, children = {
    \\      { text = "Branch B", font_size = 20, text_color = 0x70a1ff },
    \\      { style = { gap = 4 }, children = {
    \\        { style = { height = 20, background_color = 0x1e90ff, border_radius = 4 } },
    \\        { style = { height = 20, background_color = 0x5352ed, border_radius = 4 } }
    \\      } },
    \\      { style = { padding = 8 }, lua_on_press = "setTaps(taps + 10)", children = {
    \\        { text = "B taps: " .. tostring(taps), font_size = 12, text_color = 0x747d8c }
    \\      } }
    \\    } } or nil
    \\  } }
    \\end
    \\
    \\function __render()
    \\  __clearLuaNodes()
    \\  local tree = App()
    \\  if __mw0 then __declareChildren(__mw0, { tree }) end
    \\end
    \\
;

