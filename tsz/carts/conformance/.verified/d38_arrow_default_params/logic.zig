//! Generated by tsz compiler — d38_arrow_default_params [logic.zig]
//! Source: d38_arrow_default_params.tsz


// [origin:d38_arrow_default_params:logic]
// ── Embedded JS logic ────────────────────────────────────────
pub const JS_LOGIC =
    \\var results = [];
    \\// OA initial data
    \\setResults([ { label : "" , value : 0 } ]);
    \\var output = '';
    \\function setOutput(v) { output = v; __setStateString(0, v); }
    \\var count = 0;
    \\function setCount(v) { count = v; __setState(1, v); }
    \\var log = [];
    \\
    \\function formatValue(val, prefix, suffix) {
    \\  if (prefix === undefined) prefix = '$';
    \\  if (suffix === undefined) suffix = '';
    \\  return prefix + val + suffix;
    \\}
    \\
    \\function clamp(val, lo, hi) {
    \\  if (lo === undefined) lo = 0;
    \\  if (hi === undefined) hi = 100;
    \\  if (val < lo) return lo;
    \\  if (val > hi) return hi;
    \\  return val;
    \\}
    \\
    \\function runTests() {
    \\  log = [];
    \\  log.push({ label: 'format(42)', value: 42 });
    \\  log.push({ label: 'clamp(-5)', value: clamp(-5) });
    \\  log.push({ label: 'clamp(50)', value: clamp(50) });
    \\  log.push({ label: 'clamp(150)', value: clamp(150) });
    \\  log.push({ label: 'clamp(75, 0, 80)', value: clamp(75, 0, 80) });
    \\  log.push({ label: 'clamp(75, 80, 100)', value: clamp(75, 80, 100) });
    \\  setResults(log);
    \\  setCount(log.length);
    \\  setOutput('ran ' + log.length + ' tests');
    \\}
    \\
    \\function addMore() {
    \\  for (var i = 0; i < 5; i++) {
    \\    var v = (i + 1) * 17;
    \\    log.push({ label: 'clamp(' + v + ', 10, 60)', value: clamp(v, 10, 60) });
    \\  }
    \\  setResults(log);
    \\  setCount(log.length);
    \\  setOutput('added 5 more');
    \\}
    \\
    \\runTests();
    \\function setResults(v) { results = v; __setObjArr0(v); }
    \\if (results && results.length > 0) setResults(results);
    \\
;

// [origin:d38_arrow_default_params:logic]
// ── Embedded Lua logic ───────────────────────────────────────
pub const LUA_LOGIC =
    \\
;

