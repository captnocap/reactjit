//! Generated by tsz compiler — d66_chained_calls [logic.zig]
//! Source: d66_chained_calls.tsz


// [origin:d66_chained_calls:logic]
// ── Embedded JS logic ────────────────────────────────────────
pub const JS_LOGIC =
    \\var items = [];
    \\var log = [];
    \\// OA initial data
    \\setItems([ { name : "" , processed : "" } ]);
    \\setLog([ { step : "" , result : "" } ]);
    \\var logLen = 0;
    \\function setLogLen(v) { logLen = v; __setState(0, v); }
    \\var output = '';
    \\function setOutput(v) { output = v; __setStateString(1, v); }
    \\var entries = [];
    \\var logEntries = [];
    \\
    \\function upper(s) {
    \\  return s.toUpperCase !== undefined ? s.toUpperCase() : s;
    \\}
    \\
    \\function prefix(s, p) {
    \\  return p + s;
    \\}
    \\
    \\function truncate(s, len) {
    \\  if (s.length <= len) return s;
    \\  return s.slice(0, len) + '...';
    \\}
    \\
    \\function process(name) {
    \\  var step1 = upper(name);
    \\  var step2 = prefix(step1, '[');
    \\  var step3 = step2 + ']';
    \\  var step4 = truncate(step3, 12);
    \\
    \\  logEntries.push({ step: 'upper', result: step1 });
    \\  logEntries.push({ step: 'prefix', result: step2 });
    \\  logEntries.push({ step: 'concat', result: step3 });
    \\  logEntries.push({ step: 'truncate', result: step4 });
    \\  setLog(logEntries);
    \\  setLogLen(logEntries.length);
    \\
    \\  entries.push({ name: name, processed: step4 });
    \\  setItems(entries);
    \\  setOutput('processed: ' + name + ' → ' + step4);
    \\}
    \\
    \\function processAll() {
    \\  var names = ['Alpha', 'Bravo', 'Charlie', 'DeltaForce', 'Echo'];
    \\  for (var i = 0; i < names.length; i++) {
    \\    process(names[i]);
    \\  }
    \\}
    \\function setItems(v) { items = v; __setObjArr0(v); }
    \\function setLog(v) { log = v; __setObjArr1(v); }
    \\if (items && items.length > 0) setItems(items);
    \\if (log && log.length > 0) setLog(log);
    \\
;

// [origin:d66_chained_calls:logic]
// ── Embedded Lua logic ───────────────────────────────────────
pub const LUA_LOGIC =
    \\
;

