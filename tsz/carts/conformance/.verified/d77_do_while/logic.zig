//! Generated by tsz compiler — d77_do_while [logic.zig]
//! Source: d77_do_while.tsz


// [origin:d77_do_while:logic]
// ── Embedded JS logic ────────────────────────────────────────
pub const JS_LOGIC =
    \\var results = [];
    \\// OA initial data
    \\setResults([ { label : "" , value : 0 } ]);
    \\var output = '';
    \\function setOutput(v) { output = v; __setStateString(0, v); }
    \\var total = 0;
    \\function setTotal(v) { total = v; __setState(1, v); }
    \\var log = [];
    \\
    \\function collatzSteps(n) {
    \\  var steps = 0;
    \\  var current = n;
    \\  do {
    \\    if (current % 2 === 0) {
    \\      current = current / 2;
    \\    } else {
    \\      current = current * 3 + 1;
    \\    }
    \\    steps++;
    \\  } while (current !== 1 && steps < 200);
    \\
    \\  log.push({ label: 'collatz(' + n + ')', value: steps });
    \\  setResults(log);
    \\  setTotal(log.length);
    \\  setOutput('collatz(' + n + ') = ' + steps + ' steps');
    \\}
    \\
    \\function sumUntil(limit) {
    \\  var sum = 0;
    \\  var i = 1;
    \\  do {
    \\    sum += i;
    \\    i++;
    \\  } while (sum < limit);
    \\
    \\  log.push({ label: 'sumUntil(' + limit + ')', value: sum });
    \\  setResults(log);
    \\  setTotal(log.length);
    \\  setOutput('sum reached ' + sum + ' after ' + (i - 1) + ' iterations');
    \\}
    \\
    \\function fibonacci(count) {
    \\  var a = 0;
    \\  var b = 1;
    \\  var n = 0;
    \\  do {
    \\    var temp = a + b;
    \\    a = b;
    \\    b = temp;
    \\    n++;
    \\  } while (n < count);
    \\
    \\  log.push({ label: 'fib(' + count + ')', value: a });
    \\  setResults(log);
    \\  setTotal(log.length);
    \\  setOutput('fib(' + count + ') = ' + a);
    \\}
    \\
    \\function clearLog() {
    \\  log = [];
    \\  setResults(log);
    \\  setTotal(0);
    \\  setOutput('cleared');
    \\}
    \\function setResults(v) { results = v; __setObjArr0(v); }
    \\if (results && results.length > 0) setResults(results);
    \\
;

// [origin:d77_do_while:logic]
// ── Embedded Lua logic ───────────────────────────────────────
pub const LUA_LOGIC =
    \\
;

