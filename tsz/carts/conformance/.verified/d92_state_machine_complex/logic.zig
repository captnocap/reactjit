//! Generated by tsz compiler — d92_state_machine_complex [logic.zig]
//! Source: d92_state_machine_complex.tsz


// [origin:d92_state_machine_complex:logic]
// ── Embedded JS logic ────────────────────────────────────────
pub const JS_LOGIC =
    \\var history = [];
    \\// OA initial data
    \\setHistory([ { from : 0 , to : 0 , valid : 0 , desc : "" } ]);
    \\var state = 0;
    \\function setState(v) { state = v; __setState(0, v); }
    \\var prevState = 0;
    \\function setPrevState(v) { prevState = v; __setState(1, v); }
    \\var histLen = 0;
    \\function setHistLen(v) { histLen = v; __setState(2, v); }
    \\var error = '';
    \\function setError(v) { error = v; __setStateString(3, v); }
    \\var transitions = 0;
    \\function setTransitions(v) { transitions = v; __setState(4, v); }
    \\var denied = 0;
    \\function setDenied(v) { denied = v; __setState(5, v); }
    \\var streak = 0;
    \\function setStreak(v) { streak = v; __setState(6, v); }
    \\// States: 0=idle, 1=loading, 2=ready, 3=active, 4=paused, 5=error, 6=done
    \\var log = [];
    \\
    \\function stateName(s) {
    \\  if (s === 0) return 'idle';
    \\  if (s === 1) return 'loading';
    \\  if (s === 2) return 'ready';
    \\  if (s === 3) return 'active';
    \\  if (s === 4) return 'paused';
    \\  if (s === 5) return 'error';
    \\  if (s === 6) return 'done';
    \\  return '???';
    \\}
    \\
    \\function isAllowed(from, to) {
    \\  // idle → loading
    \\  if (from === 0 && to === 1) return 1;
    \\  // loading → ready, error
    \\  if (from === 1 && (to === 2 || to === 5)) return 1;
    \\  // ready → active, idle
    \\  if (from === 2 && (to === 3 || to === 0)) return 1;
    \\  // active → paused, done, error
    \\  if (from === 3 && (to === 4 || to === 6 || to === 5)) return 1;
    \\  // paused → active, idle
    \\  if (from === 4 && (to === 3 || to === 0)) return 1;
    \\  // error → idle
    \\  if (from === 5 && to === 0) return 1;
    \\  // done → idle
    \\  if (from === 6 && to === 0) return 1;
    \\  return 0;
    \\}
    \\
    \\function tryGo(target) {
    \\  var from = state;
    \\  var valid = isAllowed(from, target);
    \\  var desc = stateName(from) + ' → ' + stateName(target);
    \\
    \\  log.push({ from: from, to: target, valid: valid, desc: desc });
    \\  setHistory(log);
    \\  setHistLen(log.length);
    \\  setTransitions(transitions + 1);
    \\
    \\  if (valid === 1) {
    \\    setPrevState(from);
    \\    setState(target);
    \\    setError('');
    \\    setStreak(streak + 1);
    \\  } else {
    \\    setError(desc + ' DENIED');
    \\    setDenied(denied + 1);
    \\    setStreak(0);
    \\  }
    \\}
    \\
    \\function reset() {
    \\  setState(0);
    \\  setPrevState(0);
    \\  setError('');
    \\  setTransitions(0);
    \\  setDenied(0);
    \\  setStreak(0);
    \\  log = [];
    \\  setHistory(log);
    \\  setHistLen(0);
    \\}
    \\function setHistory(v) { history = v; __setObjArr0(v); }
    \\if (history && history.length > 0) setHistory(history);
    \\
;

// [origin:d92_state_machine_complex:logic]
// ── Embedded Lua logic ───────────────────────────────────────
pub const LUA_LOGIC =
    \\
;

