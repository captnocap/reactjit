//! Generated by tsz compiler — d74_optional_chaining [logic.zig]
//! Source: d74_optional_chaining.tsz


// [origin:d74_optional_chaining:logic]
// ── Embedded JS logic ────────────────────────────────────────
pub const JS_LOGIC =
    \\var users = [];
    \\// OA initial data
    \\setUsers([ { name : "" , profile : { bio : "" , avatar : "" } , settings : { theme : "" } } ]);
    \\var output = '';
    \\function setOutput(v) { output = v; __setStateString(0, v); }
    \\var checks = 0;
    \\function setChecks(v) { checks = v; __setState(1, v); }
    \\var data = [
    \\  { name: 'Alice', profile: { bio: 'Developer', avatar: 'A' }, settings: { theme: 'dark' } },
    \\  { name: 'Bob', profile: { bio: '', avatar: 'B' }, settings: { theme: '' } },
    \\  { name: 'Carol', profile: { bio: 'Designer', avatar: '' }, settings: { theme: 'light' } }
    \\];
    \\setUsers(data);
    \\
    \\function checkBio(idx) {
    \\  var user = data[idx];
    \\  var bio = user?.profile?.bio;
    \\  if (bio !== undefined && bio !== '') {
    \\    setOutput(user.name + ' bio: ' + bio);
    \\  } else {
    \\    setOutput(user.name + ' has no bio');
    \\  }
    \\  setChecks(checks + 1);
    \\}
    \\
    \\function checkTheme(idx) {
    \\  var user = data[idx];
    \\  var theme = user?.settings?.theme;
    \\  if (theme !== undefined && theme !== '') {
    \\    setOutput(user.name + ' theme: ' + theme);
    \\  } else {
    \\    setOutput(user.name + ' uses default theme');
    \\  }
    \\  setChecks(checks + 1);
    \\}
    \\
    \\function safeAccess() {
    \\  // Test chaining on potentially missing nested data
    \\  var first = data?.[0];
    \\  var name = first?.name;
    \\  setOutput('first user: ' + (name !== undefined ? name : 'none'));
    \\  setChecks(checks + 1);
    \\}
    \\function setUsers(v) { users = v; __setObjArr0(v); }
    \\if (users && users.length > 0) setUsers(users);
    \\function __mapPress_0_0(idx) {
    \\  var user = users[idx];
    \\  var i = idx;
    \\  checkBio(i);
    \\}
    \\function __mapPress_0_1(idx) {
    \\  var user = users[idx];
    \\  var i = idx;
    \\  checkTheme(i);
    \\}
    \\
;

// [origin:d74_optional_chaining:logic]
// ── Embedded Lua logic ───────────────────────────────────────
pub const LUA_LOGIC =
    \\
;

