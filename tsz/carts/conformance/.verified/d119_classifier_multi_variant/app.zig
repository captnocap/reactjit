//! Generated by tsz compiler — d119_classifier_multi_variant [app.zig]
//! Source: d119_classifier_multi_variant.tsz

const std = @import("std");
const api = @import("framework/api.zig");
const layout = api;
const Node = api.Node;
const Style = api.Style;
const Color = api.Color;
const state = api.state;
const engine = api.engine;
const qjs_runtime = api.qjs_runtime;
const nodes = @import("nodes.zig");
const st = @import("state.zig");
const maps = @import("maps.zig");
const logic = @import("logic.zig");

fn _updateDynamicTexts() void {
    st._dyn_text_0 = std.fmt.bufPrint(&st._dyn_buf_0, "{d} cards loaded", .{ @as(i64, @intCast(st._oa0_len)) }) catch "";
    nodes._arr_6[2].text = st._dyn_text_0;
}


fn _updateVariants() void {
    const _v = @as(usize, api.theme.rjit_theme_active_variant());
    if (_v == 0) {
        nodes._arr_6[1].style.flex_wrap = .wrap;
        nodes._arr_6[1].style.gap = 12;
        nodes._arr_6[1].style.flex_direction = .row;
    }
    else if (_v == 1) {
        nodes._arr_6[1].style.gap = 16;
        nodes._arr_6[1].style.flex_direction = .column;
    }
    else if (_v == 2) {
        nodes._arr_6[1].style.gap = 4;
        nodes._arr_6[1].style.flex_direction = .column;
    }
    else if (_v == 3) {
        nodes._arr_6[1].style.gap = 2;
        nodes._arr_6[1].style.flex_direction = .column;
    }
    else if (_v == 4) {
        nodes._arr_6[1].style.flex_wrap = .wrap;
        nodes._arr_6[1].style.gap = 8;
        nodes._arr_6[1].style.flex_direction = .row;
    }
}

fn _appInit() void {
    st._initState();
    qjs_runtime.registerHostFn("__setObjArr0", @ptrCast(&st._oa0_unpack), 1);
    qjs_runtime.registerHostFn("__setVariant", @ptrCast(&st._setVariantHost), 1);
    _updateDynamicTexts();
    _updateVariants();
    _ = maps._pool_arena.reset(.retain_capacity);
    maps._rebuildMap0();
}

fn _appTick(now: u32) void {
    _ = now;
    if (state.isDirty()) { _updateDynamicTexts();
        _ = maps._pool_arena.reset(.retain_capacity);
        maps._rebuildMap0();
 state.clearDirty(); }
    _updateVariants();
}

export fn app_get_root() *Node { return &nodes._root; }
export fn app_get_init() ?*const fn () void { return _appInit; }
export fn app_get_tick() ?*const fn (u32) void { return _appTick; }
export fn app_get_js_logic() [*]const u8 { return logic.JS_LOGIC.ptr; }
export fn app_get_js_logic_len() usize { return logic.JS_LOGIC.len; }
export fn app_get_lua_logic() [*]const u8 { return logic.LUA_LOGIC.ptr; }
export fn app_get_lua_logic_len() usize { return logic.LUA_LOGIC.len; }
export fn app_get_title() [*:0]const u8 { return "d119_classifier_multi_variant"; }

export fn app_state_count() usize { return 0; }

pub fn main() !void {
    try engine.run(.{
        .title = "d119_classifier_multi_variant",
        .root = &nodes._root,
        .js_logic = logic.JS_LOGIC,
        .lua_logic = logic.LUA_LOGIC,
        .init = _appInit,
        .tick = _appTick,
    });
}

// ── Smith debug log ──
// DBG: [INLINE PROBE] Inlining component: CardItem bodyPos: 85 token: < kind: 41
