//! Generated by tsz compiler — d89_calculator [logic.zig]
//! Source: d89_calculator.tsz


// [origin:d89_calculator:logic]
// ── Embedded JS logic ────────────────────────────────────────
pub const JS_LOGIC =
    \\var display = '0';
    \\function setDisplay(v) { display = v; __setStateString(0, v); }
    \\var prevVal = 0;
    \\function setPrevVal(v) { prevVal = v; __setState(1, v); }
    \\var operator = 0;
    \\function setOperator(v) { operator = v; __setState(2, v); }
    \\var waitingForOperand = 0;
    \\function setWaitingForOperand(v) { waitingForOperand = v; __setState(3, v); }
    \\var history = '';
    \\function setHistory(v) { history = v; __setStateString(4, v); }
    \\function pressDigit(d) {
    \\  if (waitingForOperand === 1) {
    \\    setDisplay('' + d);
    \\    setWaitingForOperand(0);
    \\  } else {
    \\    if (display === '0') {
    \\      setDisplay('' + d);
    \\    } else {
    \\      setDisplay(display + d);
    \\    }
    \\  }
    \\}
    \\
    \\function pressOp(op) {
    \\  var current = parseInt(display);
    \\  if (operator !== 0 && waitingForOperand === 0) {
    \\    var result = evaluate(prevVal, current, operator);
    \\    setDisplay('' + result);
    \\    setPrevVal(result);
    \\    setHistory(prevVal + ' ' + opSymbol(operator) + ' ' + current + ' = ' + result);
    \\  } else {
    \\    setPrevVal(current);
    \\  }
    \\  setOperator(op);
    \\  setWaitingForOperand(1);
    \\}
    \\
    \\function pressEquals() {
    \\  if (operator === 0) return;
    \\  var current = parseInt(display);
    \\  var result = evaluate(prevVal, current, operator);
    \\  setHistory(prevVal + ' ' + opSymbol(operator) + ' ' + current + ' = ' + result);
    \\  setDisplay('' + result);
    \\  setPrevVal(0);
    \\  setOperator(0);
    \\  setWaitingForOperand(1);
    \\}
    \\
    \\function evaluate(a, b, op) {
    \\  if (op === 1) return a + b;
    \\  if (op === 2) return a - b;
    \\  if (op === 3) return a * b;
    \\  if (op === 4 && b !== 0) return Math.floor(a / b);
    \\  return 0;
    \\}
    \\
    \\function opSymbol(op) {
    \\  if (op === 1) return '+';
    \\  if (op === 2) return '-';
    \\  if (op === 3) return '*';
    \\  if (op === 4) return '/';
    \\  return '?';
    \\}
    \\
    \\function pressClear() {
    \\  setDisplay('0');
    \\  setPrevVal(0);
    \\  setOperator(0);
    \\  setWaitingForOperand(0);
    \\  setHistory('');
    \\}
    \\
;

// [origin:d89_calculator:logic]
// ── Embedded Lua logic ───────────────────────────────────────
pub const LUA_LOGIC =
    \\
;

