//! Generated by tsz compiler — d73_undefined_checks [logic.zig]
//! Source: d73_undefined_checks.tsz


// [origin:d73_undefined_checks:logic]
// ── Embedded JS logic ────────────────────────────────────────
pub const JS_LOGIC =
    \\var config = [];
    \\// OA initial data
    \\setConfig([ { key : "" , value : "" , set : 0 } ]);
    \\var output = '';
    \\function setOutput(v) { output = v; __setStateString(0, v); }
    \\var setCount = 0;
    \\function setSetCount(v) { setCount = v; __setState(1, v); }
    \\var settings = [
    \\  { key: 'theme', value: 'dark', set: 1 },
    \\  { key: 'lang', value: '', set: 0 },
    \\  { key: 'fontSize', value: '14', set: 1 },
    \\  { key: 'sidebar', value: '', set: 0 },
    \\  { key: 'notifications', value: 'on', set: 1 },
    \\  { key: 'timezone', value: '', set: 0 }
    \\];
    \\setConfig(settings);
    \\
    \\var c = 0;
    \\for (var i = 0; i < settings.length; i++) {
    \\  if (settings[i].set === 1) c++;
    \\}
    \\setSetCount(c);
    \\
    \\function getValue(key) {
    \\  for (var i = 0; i < settings.length; i++) {
    \\    if (settings[i].key === key) {
    \\      if (settings[i].value === undefined || settings[i].value === '') {
    \\        setOutput(key + ' = <not set> (using default)');
    \\      } else {
    \\        setOutput(key + ' = ' + settings[i].value);
    \\      }
    \\      return;
    \\    }
    \\  }
    \\  setOutput(key + ' not found');
    \\}
    \\
    \\function setDefault(key, defaultVal) {
    \\  for (var i = 0; i < settings.length; i++) {
    \\    if (settings[i].key === key) {
    \\      if (settings[i].value === '' || settings[i].value === undefined) {
    \\        settings[i].value = defaultVal;
    \\        settings[i].set = 1;
    \\        setConfig(settings);
    \\        c = 0;
    \\        for (var j = 0; j < settings.length; j++) {
    \\          if (settings[j].set === 1) c++;
    \\        }
    \\        setSetCount(c);
    \\        setOutput('set ' + key + ' = ' + defaultVal);
    \\      } else {
    \\        setOutput(key + ' already set to ' + settings[i].value);
    \\      }
    \\      return;
    \\    }
    \\  }
    \\}
    \\
    \\function clearValue(key) {
    \\  for (var i = 0; i < settings.length; i++) {
    \\    if (settings[i].key === key) {
    \\      settings[i].value = '';
    \\      settings[i].set = 0;
    \\      setConfig(settings);
    \\      c = 0;
    \\      for (var j = 0; j < settings.length; j++) {
    \\        if (settings[j].set === 1) c++;
    \\      }
    \\      setSetCount(c);
    \\      setOutput('cleared ' + key);
    \\      return;
    \\    }
    \\  }
    \\}
    \\function setConfig(v) { config = v; __setObjArr0(v); }
    \\if (config && config.length > 0) setConfig(config);
    \\function __mapPress_0_0(idx) {
    \\  var c = config[idx];
    \\  var i = idx;
    \\  var key = c.key;
    \\  getValue(c.key);
    \\}
    \\
;

// [origin:d73_undefined_checks:logic]
// ── Embedded Lua logic ───────────────────────────────────────
pub const LUA_LOGIC =
    \\
;

