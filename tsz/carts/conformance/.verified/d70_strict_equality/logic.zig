//! Generated by tsz compiler — d70_strict_equality [logic.zig]
//! Source: d70_strict_equality.tsz


// [origin:d70_strict_equality:logic]
// ── Embedded JS logic ────────────────────────────────────────
pub const JS_LOGIC =
    \\var items = [];
    \\// OA initial data
    \\setItems([ { name : "" , status : "" , priority : 0 } ]);
    \\var filter = 'all';
    \\function setFilter(v) { filter = v; __setStateString(0, v); }
    \\var output = '';
    \\function setOutput(v) { output = v; __setStateString(1, v); }
    \\var matchCount = 0;
    \\function setMatchCount(v) { matchCount = v; __setState(2, v); }
    \\var data = [
    \\  { name: 'Auth flow', status: 'done', priority: 2 },
    \\  { name: 'Dark mode', status: 'active', priority: 1 },
    \\  { name: 'API docs', status: 'todo', priority: 0 },
    \\  { name: 'Perf audit', status: 'active', priority: 2 },
    \\  { name: 'CI setup', status: 'done', priority: 1 },
    \\  { name: 'Mobile nav', status: 'todo', priority: 0 }
    \\];
    \\setItems(data);
    \\setMatchCount(data.length);
    \\
    \\function filterBy(f) {
    \\  setFilter(f);
    \\  if (f === 'all') {
    \\    setItems(data);
    \\    setMatchCount(data.length);
    \\    setOutput('showing all');
    \\    return;
    \\  }
    \\  var result = [];
    \\  for (var i = 0; i < data.length; i++) {
    \\    if (data[i].status === f) {
    \\      result.push(data[i]);
    \\    }
    \\  }
    \\  setItems(result);
    \\  setMatchCount(result.length);
    \\  setOutput('filtered: ' + f);
    \\}
    \\
    \\function countByPriority(p) {
    \\  var count = 0;
    \\  for (var i = 0; i < data.length; i++) {
    \\    if (data[i].priority === p) count++;
    \\  }
    \\  setOutput('priority ' + p + ': ' + count + ' items');
    \\}
    \\
    \\function findByName(name) {
    \\  var found = 'not found';
    \\  for (var i = 0; i < data.length; i++) {
    \\    if (data[i].name === name) {
    \\      found = data[i].name + ' [' + data[i].status + '] p:' + data[i].priority;
    \\      break;
    \\    }
    \\  }
    \\  setOutput(found);
    \\}
    \\
    \\function testStrictInequality() {
    \\  var mismatches = 0;
    \\  for (var i = 0; i < data.length; i++) {
    \\    if (data[i].status !== 'done') mismatches++;
    \\  }
    \\  setOutput(mismatches + ' items are not done');
    \\}
    \\function setItems(v) { items = v; __setObjArr0(v); }
    \\if (items && items.length > 0) setItems(items);
    \\
;

// [origin:d70_strict_equality:logic]
// ── Embedded Lua logic ───────────────────────────────────────
pub const LUA_LOGIC =
    \\
;

