//! Generated by tsz compiler — d37_destructuring [logic.zig]
//! Source: d37_destructuring.tsz


// [origin:d37_destructuring:logic]
// ── Embedded JS logic ────────────────────────────────────────
pub const JS_LOGIC =
    \\var items = [];
    \\// OA initial data
    \\setItems([ { name : "" , x : 0 , y : 0 , active : 0 } ]);
    \\var selected = '';
    \\function setSelected(v) { selected = v; __setStateString(0, v); }
    \\var total = 0;
    \\function setTotal(v) { total = v; __setState(1, v); }
    \\var points = [
    \\  { name: 'Origin', x: 0, y: 0, active: 1 },
    \\  { name: 'TopRight', x: 100, y: 100, active: 1 },
    \\  { name: 'Center', x: 50, y: 50, active: 0 },
    \\  { name: 'BottomLeft', x: -30, y: -40, active: 1 }
    \\];
    \\setItems(points);
    \\
    \\function selectPoint(name, x, y) {
    \\  setSelected(name + ' (' + x + ',' + y + ')');
    \\}
    \\
    \\function sumActive() {
    \\  var sum = 0;
    \\  for (var i = 0; i < items.length; i++) {
    \\    if (items[i].active === 0) continue;
    \\    sum += items[i].x + items[i].y;
    \\  }
    \\  setTotal(sum);
    \\  setSelected('active sum = ' + sum);
    \\}
    \\
    \\function findMax() {
    \\  var maxVal = 0;
    \\  var maxName = '';
    \\  for (var i = 0; i < items.length; i++) {
    \\    var dist = items[i].x * items[i].x + items[i].y * items[i].y;
    \\    if (dist > maxVal) {
    \\      maxVal = dist;
    \\      maxName = items[i].name;
    \\    }
    \\  }
    \\  setSelected('farthest: ' + maxName + ' d2=' + maxVal);
    \\}
    \\function setItems(v) { items = v; __setObjArr0(v); }
    \\if (items && items.length > 0) setItems(items);
    \\function __mapPress_0_0(idx) {
    \\  var item = items[idx];
    \\  var i = idx;
    \\  var name = item.name;
    \\  var x = item.x;
    \\  var y = item.y;
    \\  selectPoint(name,x,y);
    \\}
    \\
;

// [origin:d37_destructuring:logic]
// ── Embedded Lua logic ───────────────────────────────────────
pub const LUA_LOGIC =
    \\
;

