//! Generated by tsz compiler — s06b_kanban [logic.zig]
//! Source: s06b_kanban.tsz


// [origin:s06b_kanban:logic]
// ── Embedded JS logic ────────────────────────────────────────
pub const JS_LOGIC =
    \\var cols = [];
    \\// OA initial data
    \\setCols([ { title : "" , cards : [ { id : 0 , text : "" } ] } ]);
    \\var counts = 0;
    \\function setCounts(v) { counts = v; __setState(0, v); }
    \\var columns = [
    \\  { title: 'Todo', cards: [{ id: 1, text: 'Write tests' }, { id: 2, text: 'Fix layout bug' }] },
    \\  { title: 'In Progress', cards: [{ id: 3, text: 'Auth flow' }] },
    \\  { title: 'Done', cards: [{ id: 4, text: 'Setup repo' }] }
    \\];
    \\sync();
    \\
    \\function sync() {
    \\  setCols(columns);
    \\  setCounts([columns[0].cards.length, columns[1].cards.length, columns[2].cards.length]);
    \\}
    \\
    \\var nextId = 5;
    \\
    \\function addCard(colIdx) {
    \\  columns[colIdx].cards.push({ id: nextId, text: 'New task ' + nextId });
    \\  nextId++;
    \\  sync();
    \\}
    \\
    \\function moveCard(fromCol, cardIdx, toCol) {
    \\  if (fromCol === toCol) return;
    \\  var card = columns[fromCol].cards[cardIdx];
    \\  var result = [];
    \\  for (var i = 0; i < columns[fromCol].cards.length; i++) {
    \\    if (i !== cardIdx) result.push(columns[fromCol].cards[i]);
    \\  }
    \\  columns[fromCol].cards = result;
    \\  columns[toCol].cards.push(card);
    \\  sync();
    \\}
    \\
    \\function removeCard(colIdx, cardIdx) {
    \\  var result = [];
    \\  for (var i = 0; i < columns[colIdx].cards.length; i++) {
    \\    if (i !== cardIdx) result.push(columns[colIdx].cards[i]);
    \\  }
    \\  columns[colIdx].cards = result;
    \\  sync();
    \\}
    \\function setCols(v) { cols = v; __setObjArr0(v); }
    \\if (cols && cols.length > 0) setCols(cols);
    \\function __mapPress_0_0(idx) {
    \\  var col = cols[idx];
    \\  var colIdx = idx;
    \\  addCard(colIdx);
    \\}
    \\function __mapPress_1_0(colIdx, cardIdx) {
    \\  moveCard(colIdx,cardIdx,colIdx - 1);
    \\}
    \\function __mapPress_1_1(colIdx, cardIdx) {
    \\  moveCard(colIdx,cardIdx,colIdx + 1);
    \\}
    \\function __mapPress_1_2(colIdx, cardIdx) {
    \\  removeCard(colIdx,cardIdx);
    \\}
    \\
;

// [origin:s06b_kanban:logic]
// ── Embedded Lua logic ───────────────────────────────────────
pub const LUA_LOGIC =
    \\
;

