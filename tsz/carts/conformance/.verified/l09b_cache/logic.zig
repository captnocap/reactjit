//! Generated by tsz compiler — l09b_cache [logic.zig]
//! Source: l09b_cache.tsz


// [origin:l09b_cache:logic]
// ── Embedded JS logic ────────────────────────────────────────
pub const JS_LOGIC =
    \\var cacheView = [];
    \\var log = [];
    \\// OA initial data
    \\setCacheView([ { key : "" , val : "" , age : 0 } ]);
    \\setLog([ { msg : "" } ]);
    \\var hits = 0;
    \\function setHits(v) { hits = v; __setState(0, v); }
    \\var misses = 0;
    \\function setMisses(v) { misses = v; __setState(1, v); }
    \\var MAX_SIZE = 4;
    \\var TTL = 10;
    \\var store = [];
    \\var hitCount = 0;
    \\var missCount = 0;
    \\var now = 0;
    \\var logLines = [];
    \\
    \\function addLog(msg) {
    \\  logLines.push({ msg: msg });
    \\  setLog(logLines);
    \\}
    \\
    \\function syncView() {
    \\  var view = [];
    \\  for (var i = 0; i < store.length; i++) {
    \\    view.push({ key: store[i].key, val: JSON.stringify(store[i].val), age: now - store[i].ts });
    \\  }
    \\  setCacheView(view);
    \\}
    \\
    \\function evictExpired() {
    \\  var result = [];
    \\  for (var i = 0; i < store.length; i++) {
    \\    if (now - store[i].ts < TTL) result.push(store[i]);
    \\    else addLog('evicted (TTL) ' + store[i].key);
    \\  }
    \\  store = result;
    \\}
    \\
    \\function lruEvict() {
    \\  if (store.length >= MAX_SIZE) {
    \\    addLog('LRU evict: ' + store[0].key);
    \\    store.shift();
    \\  }
    \\}
    \\
    \\function set(key, value) {
    \\  evictExpired();
    \\  for (var i = 0; i < store.length; i++) {
    \\    if (store[i].key === key) {
    \\      store.splice(i, 1);
    \\      break;
    \\    }
    \\  }
    \\  lruEvict();
    \\  store.push({ key: key, val: value, ts: now });
    \\  syncView();
    \\  addLog('set(' + key + ', ' + JSON.stringify(value) + ')');
    \\}
    \\
    \\function get(key) {
    \\  evictExpired();
    \\  for (var i = 0; i < store.length; i++) {
    \\    if (store[i].key === key) {
    \\      var entry = store.splice(i, 1)[0];
    \\      entry.ts = now;
    \\      store.push(entry);
    \\      hitCount++;
    \\      setHits(hitCount);
    \\      syncView();
    \\      addLog('HIT ' + key + ' = ' + JSON.stringify(entry.val));
    \\      return entry.val;
    \\    }
    \\  }
    \\  missCount++;
    \\  setMisses(missCount);
    \\  addLog('MISS ' + key);
    \\  return null;
    \\}
    \\
    \\function del(key) {
    \\  var result = [];
    \\  for (var i = 0; i < store.length; i++) {
    \\    if (store[i].key !== key) result.push(store[i]);
    \\  }
    \\  store = result;
    \\  syncView();
    \\  addLog('del(' + key + ')');
    \\}
    \\
    \\function tick() {
    \\  now++;
    \\  evictExpired();
    \\  syncView();
    \\  addLog('tick t=' + now);
    \\}
    \\
    \\function demoFill() {
    \\  set('user:1', { name: 'alice' });
    \\  set('user:2', { name: 'bob' });
    \\  set('user:3', { name: 'carol' });
    \\  set('cfg:theme', 'dark');
    \\}
    \\
    \\function demoGet() { get('user:1'); get('user:2'); get('missing'); }
    \\function demoOverflow() { set('user:4', { name: 'dave' }); set('user:5', { name: 'eve' }); }
    \\function clearLog() { logLines = []; setLog(logLines); }
    \\function setCacheView(v) { cacheView = v; __setObjArr0(v); }
    \\function setLog(v) { log = v; __setObjArr1(v); }
    \\if (cacheView && cacheView.length > 0) setCacheView(cacheView);
    \\if (log && log.length > 0) setLog(log);
    \\
;

// [origin:l09b_cache:logic]
// ── Embedded Lua logic ───────────────────────────────────────
pub const LUA_LOGIC =
    \\
;

