//! Generated by tsz compiler — d127_schema_driven_form [logic.zig]
//! Source: d127_schema_driven_form.tsz


// [origin:d127_schema_driven_form:logic]
// ── Embedded JS logic ────────────────────────────────────────
pub const JS_LOGIC =
    \\// Ambient namespaces
    \\var time = {
    \\  get hour() { return new Date().getHours(); },
    \\  get minute() { return String(new Date().getMinutes()).padStart(2, "0"); },
    \\  get second() { return String(new Date().getSeconds()).padStart(2, "0"); },
    \\  get year() { return new Date().getFullYear(); },
    \\  get month() { return new Date().getMonth() + 1; },
    \\  get day() { return new Date().getDate(); },
    \\  get fps() { return typeof getFps === "function" ? getFps() : 0; },
    \\  get delta() { return 16; },
    \\  get elapsed() { return Date.now(); },
    \\  get timestamp() { return Date.now(); },
    \\};
    \\var sys = {
    \\  get user() { return typeof __os_user !== "undefined" ? __os_user : "user"; },
    \\  get uptime() { return Math.floor(Date.now() / 1000); },
    \\  get os() { return "linux"; },
    \\  get host() { return "localhost"; },
    \\  get kernel() { return "unknown"; },
    \\};
    \\var device = {
    \\  get width() { return 1280; },
    \\  get height() { return 800; },
    \\  get battery() { return 100; },
    \\  get online() { return true; },
    \\  get dpi() { return 96; },
    \\};
    \\var input = {
    \\  mouse: {
    \\    get x() { return typeof getMouseX === "function" ? getMouseX() : 0; },
    \\    get y() { return typeof getMouseY === "function" ? getMouseY() : 0; },
    \\  },
    \\  keys: { shift: false, ctrl: false, alt: false },
    \\  touch: { count: 0 },
    \\};
    \\
    \\var questions = [];
    \\var options = [];
    \\var answers = [];
    \\// OA initial data
    \\setQuestions([ { text : "" , header : "" , multiSelect : 0 , options : "" } ]);
    \\setOptions([ { qIdx : 0 , label : "" , description : "" , selected : 0 } ]);
    \\setAnswers([ { qIdx : 0 , answer : "" } ]);
    \\var activeQuestion = 0;
    \\function setActiveQuestion(v) { activeQuestion = v; __setState(0, v); }
    \\var answeredCount = 0;
    \\function setAnsweredCount(v) { answeredCount = v; __setState(1, v); }
    \\var totalQuestions = 0;
    \\function setTotalQuestions(v) { totalQuestions = v; __setState(2, v); }
    \\var submitted = 0;
    \\function setSubmitted(v) { submitted = v; __setState(3, v); }
    \\var summaryText = '';
    \\function setSummaryText(v) { summaryText = v; __setStateString(4, v); }
    \\var __jsExpr_5 = '';
    \\function __setJsExpr_5(v) { __jsExpr_5 = v; __setStateString(5, v); }
    \\var qs = [
    \\  { text: 'Which library should we use for date formatting?', header: 'Library', multiSelect: 0, options: 'date-fns|dayjs|luxon' },
    \\  { text: 'Which features do you want to enable?', header: 'Features', multiSelect: 1, options: 'dark-mode|i18n|analytics|a11y' },
    \\  { text: 'What auth method should we implement?', header: 'Auth', multiSelect: 0, options: 'JWT|OAuth2|Session|API-Key' },
    \\  { text: 'Which test frameworks should we set up?', header: 'Testing', multiSelect: 1, options: 'vitest|playwright|storybook' }
    \\];
    \\setQuestions(qs);
    \\setTotalQuestions(qs.length);
    \\
    \\var opts = [];
    \\var idx = 0;
    \\for (var i = 0; i < qs.length; i++) {
    \\  var parts = qs[i].options.split('|');
    \\  for (var j = 0; j < parts.length; j++) {
    \\    opts[idx] = { qIdx: i, label: parts[j], description: '', selected: 0 };
    \\    idx++;
    \\  }
    \\}
    \\// Add descriptions to some options
    \\opts[0].description = 'Modern, tree-shakeable, immutable';
    \\opts[1].description = '2KB, Moment-compatible API';
    \\opts[2].description = 'Rich API, timezone support, by Moment team';
    \\opts[3].description = 'System-aware theme switching';
    \\opts[4].description = 'Multi-language content support';
    \\opts[5].description = 'Usage tracking and metrics';
    \\opts[6].description = 'Screen reader and keyboard nav';
    \\opts[7].description = 'Stateless, scalable tokens';
    \\opts[8].description = 'Delegated auth, third-party login';
    \\opts[9].description = 'Server-side state, cookie-based';
    \\opts[10].description = 'Simple header-based auth';
    \\opts[11].description = 'Fast, ESM-native, Vite-powered';
    \\opts[12].description = 'E2E browser testing';
    \\opts[13].description = 'Component documentation and visual testing';
    \\setOptions(opts);
    \\
    \\var ans = [];
    \\for (var k = 0; k < qs.length; k++) {
    \\  ans[k] = { qIdx: k, answer: '' };
    \\}
    \\setAnswers(ans);
    \\
    \\function selectOption(qIdx, optIdx) {
    \\  var q = qs[qIdx];
    \\  var optLabel = opts[optIdx].label;
    \\
    \\  if (q.multiSelect === 1) {
    \\    // Toggle: if already selected, deselect; otherwise select
    \\    if (opts[optIdx].selected === 1) {
    \\      opts[optIdx].selected = 0;
    \\    } else {
    \\      opts[optIdx].selected = 1;
    \\    }
    \\    // Rebuild answer from all selected options for this question
    \\    var selected = '';
    \\    for (var i = 0; i < opts.length; i++) {
    \\      if (opts[i].qIdx === qIdx && opts[i].selected === 1) {
    \\        if (selected !== '') selected = selected + ', ';
    \\        selected = selected + opts[i].label;
    \\      }
    \\    }
    \\    ans[qIdx].answer = selected;
    \\  } else {
    \\    // Single select: deselect all others in this question, select this one
    \\    for (var j = 0; j < opts.length; j++) {
    \\      if (opts[j].qIdx === qIdx) {
    \\        opts[j].selected = 0;
    \\      }
    \\    }
    \\    opts[optIdx].selected = 1;
    \\    ans[qIdx].answer = optLabel;
    \\  }
    \\
    \\  setOptions(opts);
    \\  setAnswers(ans);
    \\
    \\  var ac = 0;
    \\  for (var m = 0; m < ans.length; m++) {
    \\    if (ans[m].answer !== '') ac++;
    \\  }
    \\  setAnsweredCount(ac);
    \\}
    \\
    \\function submitForm() {
    \\  var summary = '';
    \\  for (var i = 0; i < qs.length; i++) {
    \\    if (ans[i].answer !== '') {
    \\      summary = summary + qs[i].header + ': ' + ans[i].answer + ' | ';
    \\    } else {
    \\      summary = summary + qs[i].header + ': (skipped) | ';
    \\    }
    \\  }
    \\  setSummaryText(summary);
    \\  setSubmitted(1);
    \\}
    \\
    \\function resetForm() {
    \\  for (var i = 0; i < opts.length; i++) {
    \\    opts[i].selected = 0;
    \\  }
    \\  for (var j = 0; j < ans.length; j++) {
    \\    ans[j].answer = '';
    \\  }
    \\  setOptions(opts);
    \\  setAnswers(ans);
    \\  setAnsweredCount(0);
    \\  setSubmitted(0);
    \\  setSummaryText('');
    \\}
    \\function setQuestions(v) { questions = v; __setObjArr0(v); }
    \\function setOptions(v) { options = v; __setObjArr1(v); }
    \\function setAnswers(v) { answers = v; __setObjArr2(v); }
    \\if (questions && questions.length > 0) setQuestions(questions);
    \\if (options && options.length > 0) setOptions(options);
    \\if (answers && answers.length > 0) setAnswers(answers);
    \\function __mapPress_0_0(idx) {
    \\  var q = questions[idx];
    \\  var qi = idx;
    \\  var idx = idx;
    \\  setActiveQuestion(idx);
    \\}
    \\function __mapPress_2_0(idx) {
    \\  var opt = options[idx];
    \\  var oi = idx;
    \\  var qIdx = opt.qIdx;
    \\  var optIdx = idx;
    \\  selectOption(opt.qIdx,optIdx);
    \\}
    \\function __evalDynTexts() {
    \\  try { __setStateString(5, String(questions [ ai ] . header)); } catch(e) {}
    \\}
    \\__evalDynTexts();
    \\setInterval(__evalDynTexts, 16);
    \\
;

// [origin:d127_schema_driven_form:logic]
// ── Embedded Lua logic ───────────────────────────────────────
pub const LUA_LOGIC =
    \\
;

