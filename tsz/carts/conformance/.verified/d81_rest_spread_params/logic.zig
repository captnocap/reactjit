//! Generated by tsz compiler — d81_rest_spread_params [logic.zig]
//! Source: d81_rest_spread_params.tsz


// [origin:d81_rest_spread_params:logic]
// ── Embedded JS logic ────────────────────────────────────────
pub const JS_LOGIC =
    \\var items = [];
    \\// OA initial data
    \\setItems([ { label : "" , value : 0 } ]);
    \\var output = '';
    \\function setOutput(v) { output = v; __setStateString(0, v); }
    \\var count = 0;
    \\function setCount(v) { count = v; __setState(1, v); }
    \\var log = [];
    \\
    \\function sumAll() {
    \\  // Simulate rest params by working with arguments-like array
    \\  var total = 0;
    \\  var args = [10, 20, 30, 40, 50];
    \\  for (var i = 0; i < args.length; i++) {
    \\    total += args[i];
    \\  }
    \\  log.push({ label: 'sumAll(10..50)', value: total });
    \\  setItems(log);
    \\  setCount(log.length);
    \\  setOutput('sum = ' + total);
    \\}
    \\
    \\function mergeArrays() {
    \\  var a = [1, 2, 3];
    \\  var b = [4, 5, 6];
    \\  var c = [7, 8, 9];
    \\  // Simulate [...a, ...b, ...c]
    \\  var merged = [];
    \\  for (var i = 0; i < a.length; i++) merged.push(a[i]);
    \\  for (var j = 0; j < b.length; j++) merged.push(b[j]);
    \\  for (var k = 0; k < c.length; k++) merged.push(c[k]);
    \\
    \\  var sum = 0;
    \\  for (var m = 0; m < merged.length; m++) {
    \\    sum += merged[m];
    \\  }
    \\  log.push({ label: 'merge 3 arrays', value: sum });
    \\  setItems(log);
    \\  setCount(log.length);
    \\  setOutput('merged ' + merged.length + ' items, sum=' + sum);
    \\}
    \\
    \\function prependItem() {
    \\  // Simulate [newItem, ...existing]
    \\  var existing = [{ label: 'old1', value: 1 }, { label: 'old2', value: 2 }];
    \\  var newItem = { label: 'new', value: 99 };
    \\  var result = [newItem];
    \\  for (var i = 0; i < existing.length; i++) {
    \\    result.push(existing[i]);
    \\  }
    \\  log = result;
    \\  setItems(log);
    \\  setCount(log.length);
    \\  setOutput('prepended new item');
    \\}
    \\
    \\function clearLog() {
    \\  log = [];
    \\  setItems(log);
    \\  setCount(0);
    \\  setOutput('cleared');
    \\}
    \\function setItems(v) { items = v; __setObjArr0(v); }
    \\if (items && items.length > 0) setItems(items);
    \\
;

// [origin:d81_rest_spread_params:logic]
// ── Embedded Lua logic ───────────────────────────────────────
pub const LUA_LOGIC =
    \\
;

