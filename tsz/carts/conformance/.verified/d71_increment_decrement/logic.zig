//! Generated by tsz compiler — d71_increment_decrement [logic.zig]
//! Source: d71_increment_decrement.tsz


// [origin:d71_increment_decrement:logic]
// ── Embedded JS logic ────────────────────────────────────────
pub const JS_LOGIC =
    \\var counters = [];
    \\// OA initial data
    \\setCounters([ { label : "" , value : 0 } ]);
    \\var total = 0;
    \\function setTotal(v) { total = v; __setState(0, v); }
    \\var output = '';
    \\function setOutput(v) { output = v; __setStateString(1, v); }
    \\var ctrs = [
    \\  { label: 'A', value: 0 },
    \\  { label: 'B', value: 10 },
    \\  { label: 'C', value: 20 }
    \\];
    \\setCounters(ctrs);
    \\
    \\function incrementAll() {
    \\  var sum = 0;
    \\  for (var i = 0; i < ctrs.length; i++) {
    \\    ctrs[i].value++;
    \\    sum += ctrs[i].value;
    \\  }
    \\  setCounters(ctrs);
    \\  setTotal(sum);
    \\  setOutput('incremented all');
    \\}
    \\
    \\function decrementAll() {
    \\  var sum = 0;
    \\  for (var i = 0; i < ctrs.length; i++) {
    \\    ctrs[i].value--;
    \\    sum += ctrs[i].value;
    \\  }
    \\  setCounters(ctrs);
    \\  setTotal(sum);
    \\  setOutput('decremented all');
    \\}
    \\
    \\function incrementOne(idx) {
    \\  ctrs[idx].value++;
    \\  var sum = 0;
    \\  for (var i = 0; i < ctrs.length; i++) {
    \\    sum += ctrs[i].value;
    \\  }
    \\  setCounters(ctrs);
    \\  setTotal(sum);
    \\  setOutput('incremented ' + ctrs[idx].label);
    \\}
    \\
    \\function countUp(limit) {
    \\  var count = 0;
    \\  for (var i = 0; i < limit; i++) {
    \\    count++;
    \\  }
    \\  setOutput('counted to ' + count);
    \\  setTotal(count);
    \\}
    \\
    \\function countDown(start) {
    \\  var count = start;
    \\  while (count > 0) {
    \\    count--;
    \\  }
    \\  setOutput('counted down from ' + start + ' to ' + count);
    \\  setTotal(0);
    \\}
    \\function setCounters(v) { counters = v; __setObjArr0(v); }
    \\if (counters && counters.length > 0) setCounters(counters);
    \\function __mapPress_0_0(idx) {
    \\  var ctr = counters[idx];
    \\  var i = idx;
    \\  incrementOne(i);
    \\}
    \\
;

// [origin:d71_increment_decrement:logic]
// ── Embedded Lua logic ───────────────────────────────────────
pub const LUA_LOGIC =
    \\
;

