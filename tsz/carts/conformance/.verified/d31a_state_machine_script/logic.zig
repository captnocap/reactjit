//! Generated by tsz compiler — d31a_state_machine_script [logic.zig]
//! Source: d31a_state_machine_script.tsz


// [origin:d31a_state_machine_script:logic]
// ── Embedded JS logic ────────────────────────────────────────
pub const JS_LOGIC =
    \\var history = [];
    \\// OA initial data
    \\setHistory([ { from : "" , to : "" , valid : 0 } ]);
    \\var currentState = '';
    \\function setCurrentState(v) { currentState = v; __setStateString(0, v); }
    \\var historyLen = 0;
    \\function setHistoryLen(v) { historyLen = v; __setState(1, v); }
    \\var errorMsg = '';
    \\function setErrorMsg(v) { errorMsg = v; __setStateString(2, v); }
    \\var transitionCount = 0;
    \\function setTransitionCount(v) { transitionCount = v; __setState(3, v); }
    \\setCurrentState('idle');
    \\
    \\var transitions = {
    \\  idle: ['loading', 'error'],
    \\  loading: ['success', 'error', 'idle'],
    \\  success: ['idle', 'loading'],
    \\  error: ['idle', 'loading']
    \\};
    \\
    \\var log = [];
    \\
    \\function tryTransition(target) {
    \\  var allowed = transitions[currentState];
    \\  if (!allowed) {
    \\    setErrorMsg('unknown state: ' + currentState);
    \\    return;
    \\  }
    \\  var valid = false;
    \\  for (var i = 0; i < allowed.length; i++) {
    \\    if (allowed[i] === target) { valid = true; break; }
    \\  }
    \\  var entry = { from: currentState, to: target, valid: valid ? 1 : 0 };
    \\  log.push(entry);
    \\  setHistory(log);
    \\  setHistoryLen(log.length);
    \\  setTransitionCount(transitionCount + 1);
    \\  if (valid) {
    \\    setCurrentState(target);
    \\    setErrorMsg('');
    \\  } else {
    \\    setErrorMsg(currentState + ' -> ' + target + ' not allowed');
    \\  }
    \\}
    \\
    \\function reset() {
    \\  setCurrentState('idle');
    \\  log = [];
    \\  setHistory(log);
    \\  setHistoryLen(0);
    \\  setErrorMsg('');
    \\  setTransitionCount(0);
    \\}
    \\function setHistory(v) { history = v; __setObjArr0(v); }
    \\if (history && history.length > 0) setHistory(history);
    \\
;

// [origin:d31a_state_machine_script:logic]
// ── Embedded Lua logic ───────────────────────────────────────
pub const LUA_LOGIC =
    \\
;

