// Layout library — flex layout, text measurement, scrolling.
// The framework's own layout engine, written in intent syntax.

<layout lib>
  <zscript>
    flex
    measure
    scroll
  </zscript>
</layout>
