// VS Code clone — hardcoded file tree + code snippet
// Exercises: nested layouts, .map() over tree + lines, indent levels, tab state

var _activeTab = 0;

setInterval(function() {
  setActiveTab(_activeTab);
}, 100);

function selectTab(idx) {
  _activeTab = idx;
  setActiveTab(idx);
}
