//! Generated by Smith (mod mode) — do not edit
//! Source: m25_indexed_reverse_loops.mod.tsz
//! Module: loops

const std = @import("std");

pub const Entry = struct {
    active: bool = true,
    value: usize = 0,
};

var entries: [8]Entry = [_]Entry{.{}} ** 8;
var count: usize = 0;

pub fn sum_from_one() usize {
    var total = count - count;
    var i: usize = 1;
    while (i < count) : (i += 1) {
        if (entries[i].active == true) {
            total += entries[i].value + i;
        }
    }
    return total;
}

pub fn find_last_live() usize {
    var _i: usize = 0;
    while (_i < entries.len) : (_i += 1) {
        if (entries[(entries.len - 1 - _i)].active == true) {
            return entries[(entries.len - 1 - _i)].value;
        }
    }
    return 0;
}
