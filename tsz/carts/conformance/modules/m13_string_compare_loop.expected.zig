//! Generated by Smith (mod mode) — do not edit
//! Source: m13_string_compare_loop.mod.tsz
//! Module: lookup

const std = @import("std");

pub const Entry = struct {
    name: []const u8 = "",
    value: i32 = 0,
    active: bool = true,
};

var entries: [16]Entry = [_]Entry{.{}} ** 16;
var count: u8 = 0;

pub fn add(name: []const u8, value: i32) u8 {
    if (count >= 16) return 0;
    entries[count] = .{ .name = name, .value = value, .active = true };
    count += 1;
    return count - 1;
}

pub fn find(name: []const u8) i32 {
    var _i: usize = 0;
    while (_i < count) : (_i += 1) {
        if (entries[_i].active == true and std.mem.eql(u8, entries[_i].name, name)) {
            return entries[_i].value;
        }
    }
    return -1;
}

pub fn remove(name: []const u8) void {
    var _i: usize = 0;
    while (_i < count) : (_i += 1) {
        if (std.mem.eql(u8, entries[_i].name, name)) {
            entries[_i].active = false;
        }
    }
}
