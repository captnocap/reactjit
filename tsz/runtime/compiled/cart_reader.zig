//! ──── GENERATED FILE — DO NOT EDIT ────
//!
//! Source: tsz/runtime/tsz/cart_reader.mod.tsz
//! Generated by: tsz compile-runtime (imperative mode)
//!
//! To regenerate: tsz compile-runtime tsz/runtime/tsz/cart_reader.mod.tsz
//! To modify: edit cart_reader.mod.tsz and recompile

const std = @import("std");

inline fn asF32(val: anytype) f32 {
    return switch (@typeInfo(@TypeOf(val))) {
        .int, .comptime_int => @floatFromInt(val),
        .comptime_float => val,
        .float => if (@TypeOf(val) == f32) val else @floatCast(val),
        .optional => blk: {
            const v = val orelse 0;
            break :blk switch (@typeInfo(@TypeOf(v))) {
                .int, .comptime_int => @as(f32, @floatFromInt(v)),
                .comptime_float => @as(f32, v),
                .float => if (@TypeOf(v) == f32) v else @floatCast(v),
                else => @compileError("asF32: unsupported optional inner type"),
            };
        },
        else => @compileError("asF32: unsupported type"),
    };
}

// ── Type definitions ────────────────────────────────
pub const ManifestResult = enum { ok, file_not_found, json_error, no_json_lib, unrecognized_format, archive_detected };

// ── Module state ───────────────────────────────────
const MAX_FILE_SIZE: usize = 1024 * 1024;
const MAX_PATH_LEN: usize = 512;

// ── Functions ──────────────────────────────────────

pub fn readFile(absPath: []const u8, buf: []u8) ?usize {
    const file = std.fs.openFileAbsolute(absPath, .{  }) catch return null;
    defer file.close();
    return file.readAll(buf) catch null;
}

pub fn fileSize(absPath: []const u8) ?u64 {
    const stat = std.fs.statFileAbsolute(absPath) catch return null;
    return stat.size;
}

pub fn readManifest(path: []const u8, buf: []u8) struct {
    len;
    if (:) usize else ,;
    result;
    :;
    ManifestResult;
}

pub fn looksLikeJson(content: []const u8) bool {
    {
        content;
        while ()) : (|ch | { if ( ch == ' ' || ch == '\n' || ch == '\r' || ch == '\t') {
        }
    }
    continue;
    return ch == '{';
}
