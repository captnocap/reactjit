//! ──── GENERATED FILE — DO NOT EDIT ────
//!
//! Source: tsz/runtime/tsz/leaktest.mod.tsz
//! Generated by: tsz compile-runtime (imperative mode)
//!
//! To regenerate: tsz compile-runtime tsz/runtime/tsz/leaktest.mod.tsz
//! To modify: edit leaktest.mod.tsz and recompile

const std = @import("std");

inline fn asF32(val: anytype) f32 {
    return switch (@typeInfo(@TypeOf(val))) {
        .int, .comptime_int => @floatFromInt(val),
        .comptime_float => val,
        .float => if (@TypeOf(val) == f32) val else @floatCast(val),
        .optional => blk: {
            const v = val orelse 0;
            break :blk switch (@typeInfo(@TypeOf(v))) {
                .int, .comptime_int => @as(f32, @floatFromInt(v)),
                .comptime_float => @as(f32, v),
                .float => if (@TypeOf(v) == f32) v else @floatCast(v),
                else => @compileError("asF32: unsupported optional inner type"),
            };
        },
        else => @compileError("asF32: unsupported type"),
    };
}

// ── Module state ───────────────────────────────────
var total_leaked: u64 = 0;

// ── Functions ──────────────────────────────────────

pub fn leak64() i64 {
    const size: usize = 64 * 1024 * 1024;
    const ptr = std.c.malloc(size);
    if (ptr) |p| {
        const bytes: [*]volatile u8 = @ptrCast(p);
        var i: usize = 0;
        while (i < size) {
            bytes[i] = 0xFF;
            i += 4096;
        }
        total_leaked += 64;
        std.debug.print("[leaktest] Leaked 64MB (total: {d}MB)\n", .{ .total_leaked = total_leaked });
    }
    return @intCast(total_leaked);
}
