//! ──── GENERATED FILE — DO NOT EDIT ────
//!
//! Source: tsz/runtime/tsz/framework/inspector/panel.tsz
//! Generated by: tsz compile-runtime (imperative mode)
//!
//! To regenerate: tsz compile-runtime tsz/runtime/tsz/framework/inspector/panel.tsz
//! To modify: edit panel.tsz and recompile

const std = @import("std");

inline fn asF32(val: anytype) f32 {
    return switch (@typeInfo(@TypeOf(val))) {
        .int, .comptime_int => @floatFromInt(val),
        .comptime_float => val,
        .float => if (@TypeOf(val) == f32) val else @floatCast(val),
        .optional => blk: {
            const v = val orelse 0;
            break :blk switch (@typeInfo(@TypeOf(v))) {
                .int, .comptime_int => @as(f32, @floatFromInt(v)),
                .comptime_float => @as(f32, v),
                .float => if (@TypeOf(v) == f32) v else @floatCast(v),
                else => @compileError("asF32: unsupported optional inner type"),
            };
        },
        else => @compileError("asF32: unsupported type"),
    };
}

// ── Imports ────────────────────────────────────────
const layout = @import("layout.zig");
const Node = layout.Node;

// ── Module state ───────────────────────────────────
const overlay = @import("overlay.zig");
var enabled: bool = false;
var dragging: bool = false;
var last_mx: f32 = 0;
var last_my: f32 = 0;

// ── Functions ──────────────────────────────────────

pub fn toggle() void {
    enabled = !enabled;
    if (!enabled) {
        overlay.clearSelection();
        overlay.clearHover();
    }
}

pub fn isEnabled() bool {
    return enabled;
}

pub fn getSelectedNode() ?*Node {
    return overlay.selected_node;
}

pub fn clearSelection() void {
    overlay.clearSelection();
}

pub fn mouseMoved(mx: f32, my: f32) void {
    last_mx = mx;
    last_my = my;
}

pub fn handleClick(root: *Node, mx: f32, my: f32) bool {
    if (!enabled) {
        return false;
    }
    return overlay.selectAt(root, mx, my);
}

pub fn mouseReleased() void {
    dragging = false;
}

pub fn getAppHeight(win_h: f32) f32 {
    if (!enabled) {
        return win_h;
    }
    return win_h - getPanelHeight();
}

pub fn getPanelHeight() f32 {
    if (!enabled) {
        return 0;
    }
    return 250;
}

pub fn render() void {
    overlay.render();
}

pub fn updateHover(root: *Node, _mx: f32, _my: f32) void {
    if (!enabled) {
        return;
    }
    _ = overlay.updateHover(root, last_mx, last_my);
}
