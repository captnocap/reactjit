//! ──── GENERATED FILE — DO NOT EDIT ────
//!
//! Source: tsz/runtime/framework/mouse.tsz
//! Generated by: tsz compile-runtime
//!
//! To regenerate: tsz compile-runtime tsz/runtime/framework/mouse.tsz
//! To modify: edit mouse.tsz and recompile

const std = @import("std");
const state = @import("../../state.zig");

// ── Panel identity ───────────────────────────────────────────
pub const PANEL_ID = "mouse";

// ── State slots (offset by caller) ──────────────────────────
var slot_base: usize = 0;
pub const SLOT_COUNT: usize = 6;

// ── Public API ──────────────────────────────────────────────────

pub fn init(base: usize) void {
    slot_base = base;
}

pub fn tick() void {
}


// ── State accessors ─────────────────────────────────────────
pub fn mx() i64 { return state.getSlot(slot_base + 0); }
pub fn setMx(v: i64) void { state.setSlot(slot_base + 0, v); }
pub fn my() i64 { return state.getSlot(slot_base + 1); }
pub fn setMy(v: i64) void { state.setSlot(slot_base + 1, v); }
pub fn prevX() i64 { return state.getSlot(slot_base + 2); }
pub fn setPrevX(v: i64) void { state.setSlot(slot_base + 2, v); }
pub fn prevY() i64 { return state.getSlot(slot_base + 3); }
pub fn setPrevY(v: i64) void { state.setSlot(slot_base + 3, v); }
pub fn leftDown() i64 { return state.getSlot(slot_base + 4); }
pub fn setLeftDown(v: i64) void { state.setSlot(slot_base + 4, v); }
pub fn rightDown() i64 { return state.getSlot(slot_base + 5); }
pub fn setRightDown(v: i64) void { state.setSlot(slot_base + 5, v); }
