//! ──── GENERATED FILE — DO NOT EDIT ────
//!
//! Source: tsz/runtime/tsz/breakpoint.mod.tsz
//! Generated by: tsz compile-runtime (imperative mode)
//!
//! To regenerate: tsz compile-runtime tsz/runtime/tsz/breakpoint.mod.tsz
//! To modify: edit breakpoint.mod.tsz and recompile

const std = @import("std");

inline fn asF32(val: anytype) f32 {
    return switch (@typeInfo(@TypeOf(val))) {
        .int, .comptime_int => @floatFromInt(val),
        .comptime_float => val,
        .float => if (@TypeOf(val) == f32) val else @floatCast(val),
        .optional => blk: {
            const v = val orelse 0;
            break :blk switch (@typeInfo(@TypeOf(v))) {
                .int, .comptime_int => @as(f32, @floatFromInt(v)),
                .comptime_float => @as(f32, v),
                .float => if (@TypeOf(v) == f32) v else @floatCast(v),
                else => @compileError("asF32: unsupported optional inner type"),
            };
        },
        else => @compileError("asF32: unsupported type"),
    };
}

// ── Type definitions ────────────────────────────────
pub const Breakpoint = enum { sm, md, lg, xl };

// ── Module state ───────────────────────────────────
var current_bp: Breakpoint = .lg;
var current_width: f32 = 1280;

// ── Functions ──────────────────────────────────────

pub fn update(width: f32) void {
    current_width = width;
    if (width >= 1440) {
        current_bp = .xl;
    } else if (width >= 1024) {
        current_bp = .lg;
    } else if (width >= 640) {
        current_bp = .md;
    } else {
        current_bp = .sm;
    }
}

pub fn current() Breakpoint {
    return current_bp;
}

pub fn width() f32 {
    return current_width;
}

pub fn atLeast(bp: Breakpoint) bool {
    return @intFromEnum(current_bp) >= @intFromEnum(bp);
}

pub fn name() []const u8 {
    switch (current_bp) {
        .sm => {
            return "sm";
        },
        .md => {
            return "md";
        },
        .lg => {
            return "lg";
        },
        .xl => {
            return "xl";
        },
    }
}
