# l03_script_and_lscript_together: auto-generated from runtime snapshot
# Generated by ZIGOS_WITNESS=snapshot — do not edit by hand

# Verify all visible text on initial render
# health: 19 nodes, 10 text, 4 pressable, 0 state slots, 0 fails

# Initial render
expect "L03 — JS + Lua Together"
expect "JS Counter (QuickJS timer)"
expect "+1 (JS)"
expect "Lua Counter (LuaJIT)"
expect "+1 (Lua)"
expect "+5 to JS (Lua)"
expect "None"
expect "Reset Both (Lua)"

# Click-through: 4 pressable nodes

click "+1 (JS)"
expect "L03 — JS + Lua Together"
expect "JS Counter (QuickJS timer)"
expect "+1 (JS)"
expect "Lua Counter (LuaJIT)"
expect "+1 (Lua)"
expect "+5 to JS (Lua)"
expect "JS manual"
expect "Reset Both (Lua)"

click "+1 (Lua)"
expect "L03 — JS + Lua Together"
expect "JS Counter (QuickJS timer)"
expect "+1 (JS)"
expect "Lua Counter (LuaJIT)"
expect "+1 (Lua)"
expect "+5 to JS (Lua)"
expect "JS manual"
expect "Reset Both (Lua)"

click "+5 to JS (Lua)"
expect "L03 — JS + Lua Together"
expect "JS Counter (QuickJS timer)"
expect "+1 (JS)"
expect "Lua Counter (LuaJIT)"
expect "+1 (Lua)"
expect "+5 to JS (Lua)"
expect "JS manual"
expect "Reset Both (Lua)"

click "Reset Both (Lua)"
expect "L03 — JS + Lua Together"
expect "JS Counter (QuickJS timer)"
expect "+1 (JS)"
expect "Lua Counter (LuaJIT)"
expect "+1 (Lua)"
expect "+5 to JS (Lua)"
expect "JS manual"
expect "Reset Both (Lua)"
