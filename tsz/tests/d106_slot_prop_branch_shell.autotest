# d106_slot_prop_branch_shell: auto-generated from runtime snapshot
# Generated by ZIGOS_WITNESS=snapshot — do not edit by hand

# Verify all visible text on initial render
# health: 52 nodes, 26 text, 6 pressable, 0 state slots, 0 fails

# Initial render
expect "Slot Prop Branch Shell"
expect "Retry queue is saturating"
expect "urgent"
expect "infra"
expect "nil== 1 ? \"pinned\" : `5 unread`"
expect "Finish handoff notes for launch"
expect "quiet"
expect "growth"
expect "nil== 1 ? \"pinned\" : `1 unread`"
expect "Badge spacing regressed again"
expect "watch"
expect "core"
expect "nil== 1 ? \"pinned\" : `3 unread`"
expect "Signer rotation is overdue"
expect "nil== 1 ? \"pinned\" : `0 unread`"
expect "Docs sidebar is still too deep"
expect "nil== 1 ? \"pinned\" : `2 unread`"
expect "Need copy pass on empty states"
expect "nil== 1 ? \"pinned\" : `4 unread`"
expect "0 shell actions"

# Click-through: 6 pressable nodes

click "Retry queue is saturating"
expect "Slot Prop Branch Shell"
expect "Retry queue is saturating"
expect "urgent"
expect "infra"
expect "nil== 1 ? \"pinned\" : `5 unread`"
expect "Finish handoff notes for launch"
expect "quiet"
expect "growth"
expect "nil== 1 ? \"pinned\" : `1 unread`"
expect "Badge spacing regressed again"
expect "watch"
expect "core"
expect "nil== 1 ? \"pinned\" : `3 unread`"
expect "Signer rotation is overdue"
expect "nil== 1 ? \"pinned\" : `0 unread`"
expect "Docs sidebar is still too deep"
expect "nil== 1 ? \"pinned\" : `2 unread`"
expect "Need copy pass on empty states"
expect "nil== 1 ? \"pinned\" : `4 unread`"
expect "1 shell actions"

click "Finish handoff notes for launch"
expect "Slot Prop Branch Shell"
expect "Retry queue is saturating"
expect "urgent"
expect "infra"
expect "nil== 1 ? \"pinned\" : `5 unread`"
expect "Finish handoff notes for launch"
expect "quiet"
expect "growth"
expect "nil== 1 ? \"pinned\" : `1 unread`"
expect "Badge spacing regressed again"
expect "watch"
expect "core"
expect "nil== 1 ? \"pinned\" : `3 unread`"
expect "Signer rotation is overdue"
expect "nil== 1 ? \"pinned\" : `0 unread`"
expect "Docs sidebar is still too deep"
expect "nil== 1 ? \"pinned\" : `2 unread`"
expect "Need copy pass on empty states"
expect "nil== 1 ? \"pinned\" : `4 unread`"
expect "2 shell actions"

click "Badge spacing regressed again"
expect "Slot Prop Branch Shell"
expect "Retry queue is saturating"
expect "urgent"
expect "infra"
expect "nil== 1 ? \"pinned\" : `5 unread`"
expect "Finish handoff notes for launch"
expect "quiet"
expect "growth"
expect "nil== 1 ? \"pinned\" : `1 unread`"
expect "Badge spacing regressed again"
expect "watch"
expect "core"
expect "nil== 1 ? \"pinned\" : `3 unread`"
expect "Signer rotation is overdue"
expect "nil== 1 ? \"pinned\" : `0 unread`"
expect "Docs sidebar is still too deep"
expect "nil== 1 ? \"pinned\" : `2 unread`"
expect "Need copy pass on empty states"
expect "nil== 1 ? \"pinned\" : `4 unread`"
expect "3 shell actions"

click "Signer rotation is overdue"
expect "Slot Prop Branch Shell"
expect "Retry queue is saturating"
expect "urgent"
expect "infra"
expect "nil== 1 ? \"pinned\" : `5 unread`"
expect "Finish handoff notes for launch"
expect "quiet"
expect "growth"
expect "nil== 1 ? \"pinned\" : `1 unread`"
expect "Badge spacing regressed again"
expect "watch"
expect "core"
expect "nil== 1 ? \"pinned\" : `3 unread`"
expect "Signer rotation is overdue"
expect "nil== 1 ? \"pinned\" : `0 unread`"
expect "Docs sidebar is still too deep"
expect "nil== 1 ? \"pinned\" : `2 unread`"
expect "Need copy pass on empty states"
expect "nil== 1 ? \"pinned\" : `4 unread`"
expect "4 shell actions"

click "Docs sidebar is still too deep"
expect "Slot Prop Branch Shell"
expect "Retry queue is saturating"
expect "urgent"
expect "infra"
expect "nil== 1 ? \"pinned\" : `5 unread`"
expect "Finish handoff notes for launch"
expect "quiet"
expect "growth"
expect "nil== 1 ? \"pinned\" : `1 unread`"
expect "Badge spacing regressed again"
expect "watch"
expect "core"
expect "nil== 1 ? \"pinned\" : `3 unread`"
expect "Signer rotation is overdue"
expect "nil== 1 ? \"pinned\" : `0 unread`"
expect "Docs sidebar is still too deep"
expect "nil== 1 ? \"pinned\" : `2 unread`"
expect "Need copy pass on empty states"
expect "nil== 1 ? \"pinned\" : `4 unread`"
expect "5 shell actions"

click "Need copy pass on empty states"
expect "Slot Prop Branch Shell"
expect "Retry queue is saturating"
expect "urgent"
expect "infra"
expect "nil== 1 ? \"pinned\" : `5 unread`"
expect "Finish handoff notes for launch"
expect "quiet"
expect "growth"
expect "nil== 1 ? \"pinned\" : `1 unread`"
expect "Badge spacing regressed again"
expect "watch"
expect "core"
expect "nil== 1 ? \"pinned\" : `3 unread`"
expect "Signer rotation is overdue"
expect "nil== 1 ? \"pinned\" : `0 unread`"
expect "Docs sidebar is still too deep"
expect "nil== 1 ? \"pinned\" : `2 unread`"
expect "Need copy pass on empty states"
expect "nil== 1 ? \"pinned\" : `4 unread`"
expect "6 shell actions"
