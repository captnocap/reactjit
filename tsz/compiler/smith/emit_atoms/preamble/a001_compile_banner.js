// ── Emit Atom 001: Compile banner ───────────────────────────────
// Index: 1
// Group: preamble
// Target: zig
// Status: complete
// Current owner: emit/preamble.js
//
// Trigger: every emitOutput() call.
// Output target: generated-file banner and source/lane comments.

function applies(ctx, meta) {
  void ctx;
  return typeof meta.basename === 'string';
}

function emit(ctx, meta) {
  void ctx;
  return `//! Generated by tsz compiler (Zig) \u2014 do not edit\n//!\n//! Source: ${meta.basename}\n//! Lane: ${meta.pfLane}\n\n`;
}

module.exports = {
  id: 1,
  name: 'compile_banner',
  group: 'preamble',
  target: 'zig',
  status: 'complete',
  currentOwner: 'emit/preamble.js',
  applies: applies,
  emit: emit,
};
