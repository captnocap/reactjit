/**
 * Layout 1 — Component documentation page template.
 *
 * This is a TEMPLATE for scaffolding new component stories.
 * No docKey is passed — all sections show placeholder content.
 * When a real story is scaffolded, it passes docKey="componentname"
 * and the doc sections auto-populate from content.json.
 */

import React from 'react';
import { ComponentDoc } from './_shared/ComponentDoc';

export function Layout1Story() {
  return <ComponentDoc />;
}
