# The Sequencer (and the canvas, and the contract)

This document was reorganized into a retrieval-friendly corpus
under [`docs/`](docs/README.md). The split is by topic, one
self-contained chunk per file, cross-referenced by exact filename so
embedding-driven retrieval can chain hops cleanly.

**Start at [`docs/README.md`](docs/README.md)** — it carries the
spine, the index, the user flow, the frozen positions, and
pointers into the rest.

## The spine, in one sentence

> The sequencer is build-time. The plan is runtime. The prose is
> the seam.

## What's in the corpus

- `docs/README.md` — index + spine + user flow + frozen positions.
- `docs/01-console-cartridges.md` — `cart/app/` as the console
  shell; sweatshop / gallery / chatbot as cartridges; ABI as fence.
- `docs/02-canvas-and-substrates.md` — n8n-style canvas; Composition
  (structural) + useIFTTT (reactive) as two coexisting substrates.
- `docs/03-sequencer-plan-trace.md` — toggle grid, animation as
  commit ceremony, deterministic plan, two-reader output.
- `docs/04-cells-and-tiers.md` — what a cell is, T0–T4 tier system,
  three-layer modifier surface.
- `docs/05-pathology-catalog.md` — named pathologies, 12
  trust-nothing mechanical checks, classifier topology.
- `docs/06-laws-and-promotion.md` — wounds → laws gradient,
  constitutional laws, the Green Standard.
- `docs/07-supervision-vocabulary.md` — plan/task schemas, blindness
  Privacy mode, retirement policy, hook layer, M3A memory layers,
  cockpit shape.
- `docs/08-recipes.md` — recipe file shape (graph + arming),
  Strict Supervisor recipe, default reviewer cell.
- `docs/09-the-four-principles.md` — design constraints that bind
  cell authoring.
- `docs/99-open-questions.md` — committed positions + genuine opens.

The split was made when the design was stable enough to formalize.
The corpus is the durable form; this file is the breadcrumb back
to it.
