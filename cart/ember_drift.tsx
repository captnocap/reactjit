// Themed ember-drift background — see runtime/background.tsx for the shader.

import { Background } from '@reactjit/runtime/background';

export default function EmberDrift() {
  return <Background type="ember" />;
}
