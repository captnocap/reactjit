// Themed scan-field background — see runtime/background.tsx for the shader.

import { Background } from '@reactjit/runtime/background';

export default function ScanField() {
  return <Background type="scan" />;
}
